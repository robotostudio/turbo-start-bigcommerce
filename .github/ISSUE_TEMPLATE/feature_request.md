---
name: Feature request
about: Suggest something the starter should do
labels: enhancement
---

## What's missing

## Why it belongs in a starter

People clone this and then edit it. Anything that only makes sense for one shop is better as a change
in your fork than as a feature here — say what makes this general.

## Which part it touches

- [ ] Storefront (`apps/web`)
- [ ] Studio schemas or page builder (`apps/studio`)
- [ ] A shared package (`packages/`)
- [ ] The commerce layer (`lib/shopify` today, `lib/bigcommerce` after the flip)

Requests against the commerce layer may already be covered by the BigCommerce conversion. Check
[SPEC.md](https://github.com/robotostudio/turbo-start-bigcommerce/blob/main/SPEC.md) before writing
a long one.

## Anything close to it already

Point at the file, block, or schema that nearly does this, if there is one.
