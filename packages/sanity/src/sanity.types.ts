/**
 * ---------------------------------------------------------------------------------
 * This file has been generated by Sanity TypeGen.
 * Command: `sanity typegen generate`
 *
 * Any modifications made directly to this file will be overwritten the next time
 * the TypeScript definitions are generated. Please make changes to the Sanity
 * schema definitions and/or GROQ queries if you need to update these types.
 *
 * For more information on how to use Sanity TypeGen, visit the official documentation:
 * https://www.sanity.io/docs/sanity-typegen
 * ---------------------------------------------------------------------------------
 */

// Source: schema.json
export type Link = {
  title?: string;
  description?: string;
  url?: CustomUrl;
};

export type SanityImageAssetReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "sanity.imageAsset";
};

export type SeoImage = {
  asset?: SanityImageAssetReference;
  media?: unknown; // Unable to locate the referenced type "media" in schema
  hotspot?: SanityImageHotspot;
  crop?: SanityImageCrop;
  _type: "image";
};

export type ImageLinkCardImage = {
  asset?: SanityImageAssetReference;
  media?: unknown; // Unable to locate the referenced type "imageLinkCard.image.media" in schema
  hotspot?: SanityImageHotspot;
  crop?: SanityImageCrop;
  alt?: string;
  _type: "image";
};

export type SubscribeNewsletter = {
  _type: "subscribeNewsletter";
  title?: string;
  subTitle?: Array<{
    children?: Array<{
      marks?: Array<string>;
      text?: string;
      _type: "span";
      _key: string;
    }>;
    style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
    listItem?: "number" | "bullet";
    markDefs?: Array<{
      customLink?: CustomUrl;
      _type: "customLink";
      _key: string;
    }>;
    level?: number;
    _type: "block";
    _key: string;
  }>;
  helperText?: Array<{
    children?: Array<{
      marks?: Array<string>;
      text?: string;
      _type: "span";
      _key: string;
    }>;
    style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
    listItem?: "number" | "bullet";
    markDefs?: Array<{
      customLink?: CustomUrl;
      _type: "customLink";
      _key: string;
    }>;
    level?: number;
    _type: "block";
    _key: string;
  }>;
  image?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    alt?: string;
    _type: "image";
  };
};

export type BigcommerceProductReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "bigcommerceProduct";
};

export type LayersShowcase = {
  _type: "layersShowcase";
  heading: string;
  description?: string;
  product: BigcommerceProductReference;
};

export type ImageLinkCards = {
  _type: "imageLinkCards";
  eyebrow?: string;
  title: string;
  richText?: Array<{
    children?: Array<{
      marks?: Array<string>;
      text?: string;
      _type: "span";
      _key: string;
    }>;
    style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
    listItem?: "number" | "bullet";
    markDefs?: Array<{
      customLink?: CustomUrl;
      _type: "customLink";
      _key: string;
    }>;
    level?: number;
    _type: "block";
    _key: string;
  }>;
  buttons?: Array<
    {
      _key: string;
    } & Button
  >;
  cards?: Array<{
    title: string;
    description: string;
    image?: ImageLinkCardImage;
    url?: CustomUrl;
    _type: "imageLinkCard";
    _key: string;
  }>;
};

export type Hero = {
  _type: "hero";
  style?: "classic" | "fullBleed";
  contentPosition?: "bottomLeft" | "bottomCenter" | "bottomRight";
  badge?: string;
  title?: string;
  richText?: Array<{
    children?: Array<{
      marks?: Array<string>;
      text?: string;
      _type: "span";
      _key: string;
    }>;
    style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
    listItem?: "number" | "bullet";
    markDefs?: Array<{
      customLink?: CustomUrl;
      _type: "customLink";
      _key: string;
    }>;
    level?: number;
    _type: "block";
    _key: string;
  }>;
  image?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    alt?: string;
    _type: "image";
  };
  buttons?: Array<
    {
      _key: string;
    } & Button
  >;
};

export type FaqReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "faq";
};

export type FaqCategories = {
  _type: "faqCategories";
  title: string;
  categories: Array<{
    title: string;
    faqs: Array<
      {
        _key: string;
      } & FaqReference
    >;
    _type: "faqCategory";
    _key: string;
  }>;
};

export type FaqAccordion = {
  _type: "faqAccordion";
  eyebrow?: string;
  title: string;
  subtitle?: string;
  link?: Link;
  faqs: Array<
    {
      _key: string;
    } & FaqReference
  >;
};

export type FeaturedProducts = {
  _type: "featuredProducts";
  heading?: string;
  products?: Array<
    {
      _key: string;
    } & BigcommerceProductReference
  >;
};

export type FeatureCardsIcon = {
  _type: "featureCardsIcon";
  eyebrow?: string;
  title?: string;
  richText?: Array<{
    children?: Array<{
      marks?: Array<string>;
      text?: string;
      _type: "span";
      _key: string;
    }>;
    style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
    listItem?: "number" | "bullet";
    markDefs?: Array<{
      customLink?: CustomUrl;
      _type: "customLink";
      _key: string;
    }>;
    level?: number;
    _type: "block";
    _key: string;
  }>;
  cards?: Array<{
    icon?: LucideIcon;
    title?: string;
    richText?: Array<{
      children?: Array<{
        marks?: Array<string>;
        text?: string;
        _type: "span";
        _key: string;
      }>;
      style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
      listItem?: "number" | "bullet";
      markDefs?: Array<{
        customLink?: CustomUrl;
        _type: "customLink";
        _key: string;
      }>;
      level?: number;
      _type: "block";
      _key: string;
    }>;
    _type: "featureCardIcon";
    _key: string;
  }>;
};

export type ExploreCategories = {
  _type: "exploreCategories";
  title: string;
  collections?: CollectionLinks;
  buttons?: Array<
    {
      _key: string;
    } & Button
  >;
};

export type BigcommerceCategoryReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "bigcommerceCategory";
};

export type EditorialTwoUp = {
  _type: "editorialTwoUp";
  items?: Array<{
    swatchColor?: string;
    collection: BigcommerceCategoryReference;
    _type: "editorialItem";
    _key: string;
  }>;
};

export type Cta = {
  _type: "cta";
  eyebrow?: string;
  title?: string;
  richText?: Array<{
    children?: Array<{
      marks?: Array<string>;
      text?: string;
      _type: "span";
      _key: string;
    }>;
    style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
    listItem?: "number" | "bullet";
    markDefs?: Array<{
      customLink?: CustomUrl;
      _type: "customLink";
      _key: string;
    }>;
    level?: number;
    _type: "block";
    _key: string;
  }>;
  buttons?: Array<
    {
      _key: string;
    } & Button
  >;
};

export type CollectionBanner = {
  _type: "collectionBanner";
  eyebrow?: string;
  title: string;
  description?: string;
  image?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    alt?: string;
    _type: "image";
  };
  buttons?: Array<
    {
      _key: string;
    } & Button
  >;
};

export type PageBuilder = Array<
  | ({
      _key: string;
    } & CollectionBanner)
  | ({
      _key: string;
    } & Cta)
  | ({
      _key: string;
    } & EditorialTwoUp)
  | ({
      _key: string;
    } & ExploreCategories)
  | ({
      _key: string;
    } & FeatureCardsIcon)
  | ({
      _key: string;
    } & FeaturedProducts)
  | ({
      _key: string;
    } & FaqAccordion)
  | ({
      _key: string;
    } & FaqCategories)
  | ({
      _key: string;
    } & Hero)
  | ({
      _key: string;
    } & ImageLinkCards)
  | ({
      _key: string;
    } & LayersShowcase)
  | ({
      _key: string;
    } & SubscribeNewsletter)
>;

export type Button = {
  _type: "button";
  variant?: "default" | "secondary" | "outline" | "link";
  text?: string;
  url?: CustomUrl;
};

export type RichText = Array<
  | {
      children?: Array<{
        marks?: Array<string>;
        text?: string;
        _type: "span";
        _key: string;
      }>;
      style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
      listItem?: "number" | "bullet";
      markDefs?: Array<{
        customLink?: CustomUrl;
        _type: "customLink";
        _key: string;
      }>;
      level?: number;
      _type: "block";
      _key: string;
    }
  | {
      asset?: SanityImageAssetReference;
      media?: unknown;
      hotspot?: SanityImageHotspot;
      crop?: SanityImageCrop;
      caption?: string;
      _type: "image";
      _key: string;
    }
  | ({
      _key: string;
    } & Accordion)
  | ({
      _key: string;
    } & Callout)
  | ({
      _key: string;
    } & ImageWithProductHotspots)
  | ({
      _key: string;
    } & Instagram)
>;

export type BlogReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "blog";
};

export type BlogIndexReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "blogIndex";
};

export type PageReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "page";
};

export type CustomUrl = {
  _type: "customUrl";
  type: "internal" | "external" | "email" | "product";
  openInNewTab?: boolean;
  external?: string;
  internal?:
    | BlogReference
    | BlogIndexReference
    | PageReference
    | BigcommerceProductReference
    | BigcommerceCategoryReference;
  email?: string;
  product?: BigcommerceProductReference;
  href?: string;
};

export type HomePageReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "homePage";
};

export type LinkInternal = {
  _type: "linkInternal";
  reference:
    | BigcommerceCategoryReference
    | HomePageReference
    | PageReference
    | BigcommerceProductReference;
};

export type LinkExternal = {
  _type: "linkExternal";
  url: string;
  newWindow?: boolean;
};

export type LinkEmail = {
  _type: "linkEmail";
  email?: string;
};

export type Spot = {
  _type: "spot";
  productWithVariant?: ProductWithVariantReference;
  x: number;
  y: number;
};

export type Seo = {
  _type: "seo";
  title?: string;
  description?: string;
  image?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    _type: "image";
  };
};

export type SanityImageCrop = {
  _type: "sanity.imageCrop";
  top: number;
  bottom: number;
  left: number;
  right: number;
};

export type SanityImageHotspot = {
  _type: "sanity.imageHotspot";
  x: number;
  y: number;
  height: number;
  width: number;
};

export type BigcommerceProductVariantReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "bigcommerceProductVariant";
};

export type ProductWithVariantReference = {
  _type: "productWithVariantReference";
  product?: BigcommerceProductReference;
  variant?: BigcommerceProductVariantReference;
};

export type ProductHotspots = Array<
  {
    _key: string;
  } & Spot
>;

export type ColorThemeReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "colorTheme";
};

export type NotFoundPage = {
  _type: "notFoundPage";
  title: string;
  body?: string;
  collection?: BigcommerceCategoryReference;
  colorTheme?: ColorThemeReference;
};

export type Menu = {
  _type: "menu";
  links?: MenuLinks;
};

export type MenuLinks = Array<
  | ({
      _key: string;
    } & CollectionGroup)
  | ({
      _key: string;
    } & LinkInternal)
  | ({
      _key: string;
    } & LinkExternal)
>;

export type Instagram = {
  _type: "instagram";
  url?: string;
};

export type ImageWithProductHotspots = {
  _type: "imageWithProductHotspots";
  image: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    _type: "image";
  };
  showHotspots?: boolean;
  productHotspots?: ProductHotspots;
};

export type FooterSettings = {
  _type: "footerSettings";
  links?: Array<
    | ({
        _key: string;
      } & LinkInternal)
    | ({
        _key: string;
      } & LinkExternal)
  >;
  text?: Array<{
    children?: Array<{
      marks?: Array<string>;
      text?: string;
      _type: "span";
      _key: string;
    }>;
    style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
    listItem?: "number" | "bullet";
    markDefs?: Array<{
      customLink?: CustomUrl;
      _type: "customLink";
      _key: string;
    }>;
    level?: number;
    _type: "block";
    _key: string;
  }>;
};

export type CollectionReference = {
  _type: "collectionReference";
  collection: BigcommerceCategoryReference;
  showBackground?: boolean;
};

export type CollectionLinks = Array<
  {
    _key: string;
  } & BigcommerceCategoryReference
>;

export type CollectionGroup = {
  _type: "collectionGroup";
  title: string;
  collectionLinks?: CollectionLinks;
  collectionProducts?: BigcommerceCategoryReference;
};

export type CallToAction = {
  _type: "callToAction";
  layout: "left" | "right";
  title: string;
  portableText?: string;
  link?: Array<
    | ({
        _key: string;
      } & LinkInternal)
    | ({
        _key: string;
      } & LinkExternal)
  >;
  content: Array<
    | {
        asset?: SanityImageAssetReference;
        media?: unknown;
        hotspot?: SanityImageHotspot;
        crop?: SanityImageCrop;
        _type: "image";
        _key: string;
      }
    | ({
        _key: string;
      } & ProductWithVariantReference)
  >;
};

export type Callout = {
  _type: "callout";
  text: string;
  link?: Array<
    | ({
        _key: string;
      } & LinkInternal)
    | ({
        _key: string;
      } & LinkExternal)
  >;
};

export type Accordion = {
  _type: "accordion";
  groups?: Array<
    {
      _key: string;
    } & AccordionGroup
  >;
};

export type AccordionGroup = {
  _type: "accordionGroup";
  title: string;
  body?: Array<{
    children?: Array<{
      marks?: Array<string>;
      text?: string;
      _type: "span";
      _key: string;
    }>;
    style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
    listItem?: "number" | "bullet";
    markDefs?: Array<{
      customLink?: CustomUrl;
      _type: "customLink";
      _key: string;
    }>;
    level?: number;
    _type: "block";
    _key: string;
  }>;
};

export type BigcommerceProductVariant = {
  _id: string;
  _type: "bigcommerceProductVariant";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  store?: {
    entityId?: number;
    productEntityId?: number;
    title?: string;
    sku?: string;
    price?: number;
    salePrice?: number;
    inventoryLevel?: number;
    purchasingDisabled?: boolean;
    imageUrl?: string;
    isDeleted?: boolean;
    optionValues?: Array<{
      name?: string;
      label?: string;
      _key: string;
    }>;
  };
};

export type BigcommerceProduct = {
  _id: string;
  _type: "bigcommerceProduct";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  store?: {
    entityId?: number;
    title?: string;
    slug?: Slug;
    sku?: string;
    descriptionHtml?: string;
    price?: number;
    salePrice?: number;
    isVisible?: boolean;
    isDeleted?: boolean;
    brandId?: number;
    categoryEntityIds?: Array<number>;
    variantEntityIds?: Array<number>;
    optionNames?: Array<string>;
    previewImageUrl?: string;
    createdAt?: string;
    modifiedAt?: string;
  };
  body?: Array<
    | {
        children?: Array<{
          marks?: Array<string>;
          text?: string;
          _type: "span";
          _key: string;
        }>;
        style?:
          | "normal"
          | "h1"
          | "h2"
          | "h3"
          | "h4"
          | "h5"
          | "h6"
          | "blockquote";
        listItem?: "bullet" | "number";
        markDefs?: Array<{
          href?: string;
          _type: "link";
          _key: string;
        }>;
        level?: number;
        _type: "block";
        _key: string;
      }
    | {
        asset?: SanityImageAssetReference;
        media?: unknown;
        hotspot?: SanityImageHotspot;
        crop?: SanityImageCrop;
        _type: "image";
        _key: string;
      }
    | ({
        _key: string;
      } & Accordion)
    | ({
        _key: string;
      } & Callout)
    | ({
        _key: string;
      } & ImageWithProductHotspots)
    | ({
        _key: string;
      } & Instagram)
  >;
};

export type Redirect = {
  _id: string;
  _type: "redirect";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  status?: "active" | "inactive";
  source: Slug;
  destination: Slug;
  permanent?: "true" | "false";
};

export type Slug = {
  _type: "slug";
  current: string;
  source?: string;
};

export type PromoBanner = {
  _id: string;
  _type: "promoBanner";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  enabled?: boolean;
  text: string;
  link?: CustomUrl;
};

export type Navbar = {
  _id: string;
  _type: "navbar";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  label: string;
  columns?: Array<
    | {
        title?: string;
        links: Array<{
          icon?: LucideIcon;
          name?: string;
          description?: string;
          url?: CustomUrl;
          _type: "navbarColumnLink";
          _key: string;
        }>;
        _type: "navbarColumn";
        _key: string;
      }
    | {
        name?: string;
        url?: CustomUrl;
        _type: "navbarLink";
        _key: string;
      }
    | ({
        _key: string;
      } & CollectionGroup)
  >;
  buttons?: Array<
    {
      _key: string;
    } & Button
  >;
};

export type Footer = {
  _id: string;
  _type: "footer";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  label: string;
  subtitle?: string;
  backgroundImage?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    _type: "image";
  };
  columns?: Array<{
    title?: string;
    links?: Array<{
      name?: string;
      url?: CustomUrl;
      _type: "footerColumnLink";
      _key: string;
    }>;
    _type: "footerColumn";
    _key: string;
  }>;
};

export type Settings = {
  _id: string;
  _type: "settings";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  label: string;
  siteTitle: string;
  siteDescription: string;
  logo?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    _type: "image";
  };
  contactEmail?: string;
  socialLinks?: {
    linkedin?: string;
    facebook?: string;
    twitter?: string;
    instagram?: string;
    youtube?: string;
  };
};

export type CollectionsIndex = {
  _id: string;
  _type: "collectionsIndex";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title?: string;
  subtitle?: string;
  heroTitle?: string;
  heroImage?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    alt?: string;
    _type: "image";
  };
  buttons?: Array<
    {
      _key: string;
    } & Button
  >;
  slug: Slug;
  seoTitle?: string;
  seoDescription?: string;
  seoImage?: SeoImage;
  ogTitle?: string;
  ogDescription?: string;
};

export type BlogIndex = {
  _id: string;
  _type: "blogIndex";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title?: string;
  description?: string;
  slug: Slug;
  displayFeaturedBlogs?: "yes" | "no";
  featuredBlogsCount?: "1" | "2" | "3";
  pageBuilder?: PageBuilder;
  seoTitle?: string;
  seoDescription?: string;
  seoImage?: SeoImage;
  ogTitle?: string;
  ogDescription?: string;
};

export type HomePage = {
  _id: string;
  _type: "homePage";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title?: string;
  description?: string;
  slug: Slug;
  pageBuilder?: PageBuilder;
  seoTitle?: string;
  seoDescription?: string;
  seoImage?: SeoImage;
  ogTitle?: string;
  ogDescription?: string;
};

export type ColorTheme = {
  _id: string;
  _type: "colorTheme";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title: string;
  text: Color;
  background: Color;
};

export type Color = {
  _type: "color";
  hex?: string;
  alpha?: number;
  hsl?: HslaColor;
  hsv?: HsvaColor;
  rgb?: RgbaColor;
};

export type Author = {
  _id: string;
  _type: "author";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  name: string;
  position?: string;
  image?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    alt?: string;
    _type: "image";
  };
  bio?: string;
};

export type Faq = {
  _id: string;
  _type: "faq";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title: string;
  richText?: Array<{
    children?: Array<{
      marks?: Array<string>;
      text?: string;
      _type: "span";
      _key: string;
    }>;
    style?: "normal" | "h2" | "h3" | "h4" | "h5" | "h6";
    listItem?: "number" | "bullet";
    markDefs?: Array<{
      customLink?: CustomUrl;
      _type: "customLink";
      _key: string;
    }>;
    level?: number;
    _type: "block";
    _key: string;
  }>;
};

export type Page = {
  _id: string;
  _type: "page";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title: string;
  description?: string;
  slug: Slug;
  image?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    alt?: string;
    _type: "image";
  };
  pageBuilder?: PageBuilder;
  seoTitle?: string;
  seoDescription?: string;
  seoImage?: SeoImage;
  seoNoIndex?: boolean;
  ogTitle?: string;
  ogDescription?: string;
};

export type LucideIcon = string;

export type BigcommerceCategory = {
  _id: string;
  _type: "bigcommerceCategory";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  store?: {
    entityId?: number;
    title?: string;
    slug?: Slug;
    descriptionHtml?: string;
    parentEntityId?: number;
    isVisible?: boolean;
    isDeleted?: boolean;
    imageUrl?: string;
  };
  body?: Array<
    | {
        children?: Array<{
          marks?: Array<string>;
          text?: string;
          _type: "span";
          _key: string;
        }>;
        style?:
          | "normal"
          | "h1"
          | "h2"
          | "h3"
          | "h4"
          | "h5"
          | "h6"
          | "blockquote";
        listItem?: "bullet" | "number";
        markDefs?: Array<{
          href?: string;
          _type: "link";
          _key: string;
        }>;
        level?: number;
        _type: "block";
        _key: string;
      }
    | {
        asset?: SanityImageAssetReference;
        media?: unknown;
        hotspot?: SanityImageHotspot;
        crop?: SanityImageCrop;
        _type: "image";
        _key: string;
      }
    | ({
        _key: string;
      } & Accordion)
    | ({
        _key: string;
      } & Callout)
    | ({
        _key: string;
      } & ImageWithProductHotspots)
    | ({
        _key: string;
      } & Instagram)
  >;
};

export type AuthorReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "author";
};

export type CategoryReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "category";
};

export type Blog = {
  _id: string;
  _type: "blog";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  orderRank?: string;
  title: string;
  description?: string;
  slug: Slug;
  authors: Array<
    {
      _key: string;
    } & AuthorReference
  >;
  category?: CategoryReference;
  publishedAt?: string;
  image: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    alt?: string;
    _type: "image";
  };
  richText?: RichText;
  seoTitle?: string;
  seoDescription?: string;
  seoImage?: SeoImage;
  seoNoIndex?: boolean;
  seoHideFromLists?: boolean;
  ogTitle?: string;
  ogDescription?: string;
};

export type Category = {
  _id: string;
  _type: "category";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  orderRank?: string;
  title: string;
  slug: Slug;
  description?: string;
};

export type SanityAssistInstructionTask = {
  _type: "sanity.assist.instructionTask";
  path?: string;
  instructionKey?: string;
  started?: string;
  updated?: string;
  info?: string;
};

export type SanityAssistTaskStatus = {
  _type: "sanity.assist.task.status";
  tasks?: Array<
    {
      _key: string;
    } & SanityAssistInstructionTask
  >;
};

export type SanityAssistSchemaTypeAnnotations = {
  _type: "sanity.assist.schemaType.annotations";
  title?: string;
  fields?: Array<
    {
      _key: string;
    } & SanityAssistSchemaTypeField
  >;
};

export type SanityAssistOutputType = {
  _type: "sanity.assist.output.type";
  type?: string;
};

export type SanityAssistOutputField = {
  _type: "sanity.assist.output.field";
  path?: string;
};

export type AssistInstructionContextReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "assist.instruction.context";
};

export type SanityAssistInstructionContext = {
  _type: "sanity.assist.instruction.context";
  reference: AssistInstructionContextReference;
};

export type AssistInstructionContext = {
  _id: string;
  _type: "assist.instruction.context";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title?: string;
  context?: Array<{
    children?: Array<{
      marks?: Array<string>;
      text?: string;
      _type: "span";
      _key: string;
    }>;
    style?: "normal";
    listItem?: never;
    markDefs?: null;
    level?: number;
    _type: "block";
    _key: string;
  }>;
};

export type SanityAssistInstructionUserInput = {
  _type: "sanity.assist.instruction.userInput";
  message: string;
  description?: string;
};

export type SanityAssistInstructionPrompt = Array<{
  children?: Array<
    | {
        marks?: Array<string>;
        text?: string;
        _type: "span";
        _key: string;
      }
    | ({
        _key: string;
      } & SanityAssistInstructionFieldRef)
    | ({
        _key: string;
      } & SanityAssistInstructionContext)
    | ({
        _key: string;
      } & SanityAssistInstructionUserInput)
  >;
  style?: "normal";
  listItem?: never;
  markDefs?: null;
  level?: number;
  _type: "block";
  _key: string;
}>;

export type SanityAssistInstructionFieldRef = {
  _type: "sanity.assist.instruction.fieldRef";
  path?: string;
};

export type SanityAssistInstruction = {
  _type: "sanity.assist.instruction";
  prompt?: SanityAssistInstructionPrompt;
  icon?: string;
  title?: string;
  userId?: string;
  createdById?: string;
  output?: Array<
    | ({
        _key: string;
      } & SanityAssistOutputField)
    | ({
        _key: string;
      } & SanityAssistOutputType)
  >;
};

export type SanityAssistSchemaTypeField = {
  _type: "sanity.assist.schemaType.field";
  path?: string;
  instructions?: Array<
    {
      _key: string;
    } & SanityAssistInstruction
  >;
};

export type MediaTag = {
  _id: string;
  _type: "media.tag";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  name?: Slug;
};

export type RgbaColor = {
  _type: "rgbaColor";
  r?: number;
  g?: number;
  b?: number;
  a?: number;
};

export type HsvaColor = {
  _type: "hsvaColor";
  h?: number;
  s?: number;
  v?: number;
  a?: number;
};

export type HslaColor = {
  _type: "hslaColor";
  h?: number;
  s?: number;
  l?: number;
  a?: number;
};

export type SanityImagePaletteSwatch = {
  _type: "sanity.imagePaletteSwatch";
  background?: string;
  foreground?: string;
  population?: number;
  title?: string;
};

export type SanityImagePalette = {
  _type: "sanity.imagePalette";
  darkMuted?: SanityImagePaletteSwatch;
  lightVibrant?: SanityImagePaletteSwatch;
  darkVibrant?: SanityImagePaletteSwatch;
  vibrant?: SanityImagePaletteSwatch;
  dominant?: SanityImagePaletteSwatch;
  lightMuted?: SanityImagePaletteSwatch;
  muted?: SanityImagePaletteSwatch;
};

export type SanityImageDimensions = {
  _type: "sanity.imageDimensions";
  height: number;
  width: number;
  aspectRatio: number;
};

export type SanityImageMetadata = {
  _type: "sanity.imageMetadata";
  location?: Geopoint;
  dimensions?: SanityImageDimensions;
  palette?: SanityImagePalette;
  lqip?: string;
  blurHash?: string;
  thumbHash?: string;
  hasAlpha?: boolean;
  isOpaque?: boolean;
};

export type SanityFileAsset = {
  _id: string;
  _type: "sanity.fileAsset";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  originalFilename?: string;
  label?: string;
  title?: string;
  description?: string;
  altText?: string;
  sha1hash?: string;
  extension?: string;
  mimeType?: string;
  size?: number;
  assetId?: string;
  uploadId?: string;
  path?: string;
  url?: string;
  source?: SanityAssetSourceData;
};

export type SanityAssetSourceData = {
  _type: "sanity.assetSourceData";
  name?: string;
  id?: string;
  url?: string;
};

export type SanityImageAsset = {
  _id: string;
  _type: "sanity.imageAsset";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  originalFilename?: string;
  label?: string;
  title?: string;
  description?: string;
  altText?: string;
  sha1hash?: string;
  extension?: string;
  mimeType?: string;
  size?: number;
  assetId?: string;
  uploadId?: string;
  path?: string;
  url?: string;
  metadata?: SanityImageMetadata;
  source?: SanityAssetSourceData;
};

export type Geopoint = {
  _type: "geopoint";
  lat?: number;
  lng?: number;
  alt?: number;
};

export type AllSanitySchemaTypes =
  | Link
  | SanityImageAssetReference
  | SeoImage
  | ImageLinkCardImage
  | SubscribeNewsletter
  | BigcommerceProductReference
  | LayersShowcase
  | ImageLinkCards
  | Hero
  | FaqReference
  | FaqCategories
  | FaqAccordion
  | FeaturedProducts
  | FeatureCardsIcon
  | ExploreCategories
  | BigcommerceCategoryReference
  | EditorialTwoUp
  | Cta
  | CollectionBanner
  | PageBuilder
  | Button
  | RichText
  | BlogReference
  | BlogIndexReference
  | PageReference
  | CustomUrl
  | HomePageReference
  | LinkInternal
  | LinkExternal
  | LinkEmail
  | Spot
  | Seo
  | SanityImageCrop
  | SanityImageHotspot
  | BigcommerceProductVariantReference
  | ProductWithVariantReference
  | ProductHotspots
  | ColorThemeReference
  | NotFoundPage
  | Menu
  | MenuLinks
  | Instagram
  | ImageWithProductHotspots
  | FooterSettings
  | CollectionReference
  | CollectionLinks
  | CollectionGroup
  | CallToAction
  | Callout
  | Accordion
  | AccordionGroup
  | BigcommerceProductVariant
  | BigcommerceProduct
  | Redirect
  | Slug
  | PromoBanner
  | Navbar
  | Footer
  | Settings
  | CollectionsIndex
  | BlogIndex
  | HomePage
  | ColorTheme
  | Color
  | Author
  | Faq
  | Page
  | LucideIcon
  | BigcommerceCategory
  | AuthorReference
  | CategoryReference
  | Blog
  | Category
  | SanityAssistInstructionTask
  | SanityAssistTaskStatus
  | SanityAssistSchemaTypeAnnotations
  | SanityAssistOutputType
  | SanityAssistOutputField
  | AssistInstructionContextReference
  | SanityAssistInstructionContext
  | AssistInstructionContext
  | SanityAssistInstructionUserInput
  | SanityAssistInstructionPrompt
  | SanityAssistInstructionFieldRef
  | SanityAssistInstruction
  | SanityAssistSchemaTypeField
  | MediaTag
  | RgbaColor
  | HsvaColor
  | HslaColor
  | SanityImagePaletteSwatch
  | SanityImagePalette
  | SanityImageDimensions
  | SanityImageMetadata
  | SanityFileAsset
  | SanityAssetSourceData
  | SanityImageAsset
  | Geopoint;

export declare const internalGroqTypeReferenceTo: unique symbol;

type ArrayOf<T> = Array<
  T & {
    _key: string;
  }
>;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryImageType
// Query: *[_type == "page" && defined(image)][0]{      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  }  }.image
export type QueryImageTypeResult = {
  id: string | null;
  preview: string | null;
  alt: string | "untitled";
  hotspot: {
    x: number;
    y: number;
  } | null;
  crop: {
    bottom: number;
    left: number;
    right: number;
    top: number;
  } | null;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryHomePageData
// Query: *[_type == "homePage" && _id == "homePage"][0]{    ...,    _id,    _type,    "slug": slug.current,    title,    description,      pageBuilder[]{    ...,    _type,      _type == "collectionBanner" => {    ...,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  }  },      _type == "cta" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },  },      _type == "editorialTwoUp" => {    ...,    "items": array::compact(items[]{      ...,      swatchColor,      "collectionTitle": collection->store.title,      "collectionImage": collection->store.imageUrl,      "collectionHref": select(        defined(collection) => "/collections/" + collection->store.slug.current,        null      ),    })  },      _type == "exploreCategories" => {    ...,      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },    // Picked, else automatic — the shape Featured Products already uses and the    // README already documents. The guard is count() > 0 rather than    // defined(collections), because an editor who adds picks and then removes    // them leaves an empty array behind, and defined([]) is true. On an absent    // field count(null) > 0 evaluates to null, which select() treats as no match    // and falls through — verified against the live dataset, not assumed.    "collections": select(      // A picked category the sync has since tombstoned or the merchant has      // hidden drops out, and if every pick is dead the block renders nothing      // rather than reverting to the automatic row. Same call as the hotspot      // spot-drop and featured-cards.ts: showing an editor four categories they      // did not choose, because the ones they did choose quietly died, is worse      // than showing none.      //      // isVisible is spelled == true rather than != false, and the strictness      // is the point: /collections/[...slug] resolves against live BigCommerce,      // which omits a hidden category from its tree, so a card this block links      // on a null flag is a card pointing at a 404 — the shape featured-cards.ts      // rules out for products. queryAllCollections filters identically for the      // same reason.      //      // array::compact for the other half of the same problem: these references      // are weak, so the target may not exist at all rather than merely being      // tombstoned — which is the normal state of a fresh install between the      // content import and the first sync. The filter does not catch that one.      // A missing document makes store.isDeleted null, null != true is true, so      // the reference passes and the deref then yields null straight into the      // array. featuredProductsBlock compacts the identical shape for the      // identical reason. It stays after the isVisible clause: the clause is      // about what the merchant published, compact is about whether the target      // document exists, and neither answers the other's question.      count(collections) > 0 => array::compact(collections[@->store.isDeleted != true && @->store.isVisible == true]->{  _id,  "title": store.title,  "slug": store.slug.current,  "imageUrl": store.imageUrl,}),      // The parentEntityId clause is what makes "top-level" true rather than      // accidental. Every synced category is flat today (ROB-2566), so it      // filters nothing yet; the moment parentage lands in the sync, without it      // this block starts returning subcategories.      //      // Ordered by title because BigCommerce's own sortOrder reaches the seed      // fixture but is never mapped into the synced document — see      // toCategoryDocument in packages/sanity-sync/src/upsert.ts. Using it would      // need a schema change and a re-sync. Title order is at least a decision;      // the _id order it replaces was an accident of insertion.      *[        _type == "bigcommerceCategory"        && defined(store.slug.current)        && store.isDeleted != true        && store.isVisible == true        && !defined(store.parentEntityId)      ] | order(store.title asc) [0...4]{  _id,  "title": store.title,  "slug": store.slug.current,  "imageUrl": store.imageUrl,}    )  },      _type == "hero" => {    ...,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  },      _type == "faqAccordion" => {    ...,      "faqs": array::compact(faqs[]->{    title,    _id,    _type,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  }),    link{      ...,      "openInNewTab": url.openInNewTab,      "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    )    }  },      _type == "faqCategories" => {    ...,    categories[]{      _key,      title,        "faqs": array::compact(faqs[]->{    title,    _id,    _type,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  })    }  },      _type == "featureCardsIcon" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },    "cards": array::compact(cards[]{      ...,        richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },    })  },      _type == "featuredProducts" => {    ...,    heading,    "productHandles": array::compact(products[]->store.slug.current)  },      _type == "layersShowcase" => {    ...,    heading,    description,    "productHandle": product->store.slug.current,    "productTitle": product->store.title,  },      _type == "subscribeNewsletter" => {    ...,    "subTitle": subTitle[]{      ...,        markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }    },    "helperText": helperText[]{      ...,        markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }    },      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  }  },      _type == "imageLinkCards" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },    "cards": array::compact(cards[]{      ...,      "openInNewTab": url.openInNewTab,      "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),        image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },    })  }  }  }
export type QueryHomePageDataResult = {
  _id: "homePage";
  _type: "homePage";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title: string | null;
  description: string | null;
  slug: string;
  pageBuilder: Array<
    | {
        _key: string;
        _type: "collectionBanner";
        eyebrow?: string;
        title: string;
        description?: string;
        image: {
          id: string | null;
          preview: string | null;
          alt: string | "untitled";
          hotspot: {
            x: number;
            y: number;
          } | null;
          crop: {
            bottom: number;
            left: number;
            right: number;
            top: number;
          } | null;
        } | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "cta";
        eyebrow?: string;
        title?: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "editorialTwoUp";
        items: Array<{
          swatchColor: string | null;
          collection: BigcommerceCategoryReference;
          _type: "editorialItem";
          _key: string;
          collectionTitle: string | null;
          collectionImage: string | null;
          collectionHref: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "exploreCategories";
        title: string;
        collections: Array<{
          _id: string;
          title: string | null;
          slug: string | null;
          imageUrl: string | null;
        }> | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "faqAccordion";
        eyebrow?: string;
        title: string;
        subtitle?: string;
        link: {
          title?: string;
          description?: string;
          url?: CustomUrl;
          openInNewTab: boolean | null;
          href: string | null;
        } | null;
        faqs: Array<{
          title: string;
          _id: string;
          _type: "faq";
          richText: Array<{
            children?: Array<{
              marks?: Array<string>;
              text?: string;
              _type: "span";
              _key: string;
            }>;
            style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
            listItem?: "bullet" | "number";
            markDefs: Array<
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                  openInNewTab: boolean | null;
                  href: string | "#" | null;
                }
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                }
            > | null;
            level?: number;
            _type: "block";
            _key: string;
          }> | null;
        }>;
      }
    | {
        _key: string;
        _type: "faqCategories";
        title: string;
        categories: Array<{
          _key: string;
          title: string;
          faqs: Array<{
            title: string;
            _id: string;
            _type: "faq";
            richText: Array<{
              children?: Array<{
                marks?: Array<string>;
                text?: string;
                _type: "span";
                _key: string;
              }>;
              style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
              listItem?: "bullet" | "number";
              markDefs: Array<
                | {
                    customLink?: CustomUrl;
                    _type: "customLink";
                    _key: string;
                    openInNewTab: boolean | null;
                    href: string | "#" | null;
                  }
                | {
                    customLink?: CustomUrl;
                    _type: "customLink";
                    _key: string;
                  }
              > | null;
              level?: number;
              _type: "block";
              _key: string;
            }> | null;
          }>;
        }>;
      }
    | {
        _key: string;
        _type: "featureCardsIcon";
        eyebrow?: string;
        title?: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        cards: Array<{
          icon?: LucideIcon;
          title?: string;
          richText: Array<{
            children?: Array<{
              marks?: Array<string>;
              text?: string;
              _type: "span";
              _key: string;
            }>;
            style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
            listItem?: "bullet" | "number";
            markDefs: Array<
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                  openInNewTab: boolean | null;
                  href: string | "#" | null;
                }
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                }
            > | null;
            level?: number;
            _type: "block";
            _key: string;
          }> | null;
          _type: "featureCardIcon";
          _key: string;
        }> | null;
      }
    | {
        _key: string;
        _type: "featuredProducts";
        heading: string | null;
        products?: Array<
          {
            _key: string;
          } & BigcommerceProductReference
        >;
        productHandles: Array<string> | null;
      }
    | {
        _key: string;
        _type: "hero";
        style?: "classic" | "fullBleed";
        contentPosition?: "bottomCenter" | "bottomLeft" | "bottomRight";
        badge?: string;
        title?: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        image: {
          id: string | null;
          preview: string | null;
          alt: string | "untitled";
          hotspot: {
            x: number;
            y: number;
          } | null;
          crop: {
            bottom: number;
            left: number;
            right: number;
            top: number;
          } | null;
        } | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "imageLinkCards";
        eyebrow?: string;
        title: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
        cards: Array<{
          title: string;
          description: string;
          image: {
            id: string | null;
            preview: string | null;
            alt: string | "untitled";
            hotspot: {
              x: number;
              y: number;
            } | null;
            crop: {
              bottom: number;
              left: number;
              right: number;
              top: number;
            } | null;
          } | null;
          url?: CustomUrl;
          _type: "imageLinkCard";
          _key: string;
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "layersShowcase";
        heading: string;
        description: string | null;
        product: BigcommerceProductReference;
        productHandle: string | null;
        productTitle: string | null;
      }
    | {
        _key: string;
        _type: "subscribeNewsletter";
        title?: string;
        subTitle: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        helperText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        image: {
          id: string | null;
          preview: string | null;
          alt: string | "untitled";
          hotspot: {
            x: number;
            y: number;
          } | null;
          crop: {
            bottom: number;
            left: number;
            right: number;
            top: number;
          } | null;
        } | null;
      }
  > | null;
  seoTitle?: string;
  seoDescription?: string;
  seoImage?: SeoImage;
  ogTitle?: string;
  ogDescription?: string;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: querySlugPageData
// Query: *[_type == "page" && slug.current == $slug][0]{    ...,    "slug": slug.current,      pageBuilder[]{    ...,    _type,      _type == "collectionBanner" => {    ...,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  }  },      _type == "cta" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },  },      _type == "editorialTwoUp" => {    ...,    "items": array::compact(items[]{      ...,      swatchColor,      "collectionTitle": collection->store.title,      "collectionImage": collection->store.imageUrl,      "collectionHref": select(        defined(collection) => "/collections/" + collection->store.slug.current,        null      ),    })  },      _type == "exploreCategories" => {    ...,      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },    // Picked, else automatic — the shape Featured Products already uses and the    // README already documents. The guard is count() > 0 rather than    // defined(collections), because an editor who adds picks and then removes    // them leaves an empty array behind, and defined([]) is true. On an absent    // field count(null) > 0 evaluates to null, which select() treats as no match    // and falls through — verified against the live dataset, not assumed.    "collections": select(      // A picked category the sync has since tombstoned or the merchant has      // hidden drops out, and if every pick is dead the block renders nothing      // rather than reverting to the automatic row. Same call as the hotspot      // spot-drop and featured-cards.ts: showing an editor four categories they      // did not choose, because the ones they did choose quietly died, is worse      // than showing none.      //      // isVisible is spelled == true rather than != false, and the strictness      // is the point: /collections/[...slug] resolves against live BigCommerce,      // which omits a hidden category from its tree, so a card this block links      // on a null flag is a card pointing at a 404 — the shape featured-cards.ts      // rules out for products. queryAllCollections filters identically for the      // same reason.      //      // array::compact for the other half of the same problem: these references      // are weak, so the target may not exist at all rather than merely being      // tombstoned — which is the normal state of a fresh install between the      // content import and the first sync. The filter does not catch that one.      // A missing document makes store.isDeleted null, null != true is true, so      // the reference passes and the deref then yields null straight into the      // array. featuredProductsBlock compacts the identical shape for the      // identical reason. It stays after the isVisible clause: the clause is      // about what the merchant published, compact is about whether the target      // document exists, and neither answers the other's question.      count(collections) > 0 => array::compact(collections[@->store.isDeleted != true && @->store.isVisible == true]->{  _id,  "title": store.title,  "slug": store.slug.current,  "imageUrl": store.imageUrl,}),      // The parentEntityId clause is what makes "top-level" true rather than      // accidental. Every synced category is flat today (ROB-2566), so it      // filters nothing yet; the moment parentage lands in the sync, without it      // this block starts returning subcategories.      //      // Ordered by title because BigCommerce's own sortOrder reaches the seed      // fixture but is never mapped into the synced document — see      // toCategoryDocument in packages/sanity-sync/src/upsert.ts. Using it would      // need a schema change and a re-sync. Title order is at least a decision;      // the _id order it replaces was an accident of insertion.      *[        _type == "bigcommerceCategory"        && defined(store.slug.current)        && store.isDeleted != true        && store.isVisible == true        && !defined(store.parentEntityId)      ] | order(store.title asc) [0...4]{  _id,  "title": store.title,  "slug": store.slug.current,  "imageUrl": store.imageUrl,}    )  },      _type == "hero" => {    ...,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  },      _type == "faqAccordion" => {    ...,      "faqs": array::compact(faqs[]->{    title,    _id,    _type,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  }),    link{      ...,      "openInNewTab": url.openInNewTab,      "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    )    }  },      _type == "faqCategories" => {    ...,    categories[]{      _key,      title,        "faqs": array::compact(faqs[]->{    title,    _id,    _type,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  })    }  },      _type == "featureCardsIcon" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },    "cards": array::compact(cards[]{      ...,        richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },    })  },      _type == "featuredProducts" => {    ...,    heading,    "productHandles": array::compact(products[]->store.slug.current)  },      _type == "layersShowcase" => {    ...,    heading,    description,    "productHandle": product->store.slug.current,    "productTitle": product->store.title,  },      _type == "subscribeNewsletter" => {    ...,    "subTitle": subTitle[]{      ...,        markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }    },    "helperText": helperText[]{      ...,        markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }    },      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  }  },      _type == "imageLinkCards" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },    "cards": array::compact(cards[]{      ...,      "openInNewTab": url.openInNewTab,      "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),        image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },    })  }  }  }
export type QuerySlugPageDataResult = {
  _id: string;
  _type: "page";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title: string;
  description?: string;
  slug: string;
  image?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    alt?: string;
    _type: "image";
  };
  pageBuilder: Array<
    | {
        _key: string;
        _type: "collectionBanner";
        eyebrow?: string;
        title: string;
        description?: string;
        image: {
          id: string | null;
          preview: string | null;
          alt: string | "untitled";
          hotspot: {
            x: number;
            y: number;
          } | null;
          crop: {
            bottom: number;
            left: number;
            right: number;
            top: number;
          } | null;
        } | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "cta";
        eyebrow?: string;
        title?: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "editorialTwoUp";
        items: Array<{
          swatchColor: string | null;
          collection: BigcommerceCategoryReference;
          _type: "editorialItem";
          _key: string;
          collectionTitle: string | null;
          collectionImage: string | null;
          collectionHref: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "exploreCategories";
        title: string;
        collections: Array<{
          _id: string;
          title: string | null;
          slug: string | null;
          imageUrl: string | null;
        }> | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "faqAccordion";
        eyebrow?: string;
        title: string;
        subtitle?: string;
        link: {
          title?: string;
          description?: string;
          url?: CustomUrl;
          openInNewTab: boolean | null;
          href: string | null;
        } | null;
        faqs: Array<{
          title: string;
          _id: string;
          _type: "faq";
          richText: Array<{
            children?: Array<{
              marks?: Array<string>;
              text?: string;
              _type: "span";
              _key: string;
            }>;
            style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
            listItem?: "bullet" | "number";
            markDefs: Array<
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                  openInNewTab: boolean | null;
                  href: string | "#" | null;
                }
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                }
            > | null;
            level?: number;
            _type: "block";
            _key: string;
          }> | null;
        }>;
      }
    | {
        _key: string;
        _type: "faqCategories";
        title: string;
        categories: Array<{
          _key: string;
          title: string;
          faqs: Array<{
            title: string;
            _id: string;
            _type: "faq";
            richText: Array<{
              children?: Array<{
                marks?: Array<string>;
                text?: string;
                _type: "span";
                _key: string;
              }>;
              style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
              listItem?: "bullet" | "number";
              markDefs: Array<
                | {
                    customLink?: CustomUrl;
                    _type: "customLink";
                    _key: string;
                    openInNewTab: boolean | null;
                    href: string | "#" | null;
                  }
                | {
                    customLink?: CustomUrl;
                    _type: "customLink";
                    _key: string;
                  }
              > | null;
              level?: number;
              _type: "block";
              _key: string;
            }> | null;
          }>;
        }>;
      }
    | {
        _key: string;
        _type: "featureCardsIcon";
        eyebrow?: string;
        title?: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        cards: Array<{
          icon?: LucideIcon;
          title?: string;
          richText: Array<{
            children?: Array<{
              marks?: Array<string>;
              text?: string;
              _type: "span";
              _key: string;
            }>;
            style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
            listItem?: "bullet" | "number";
            markDefs: Array<
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                  openInNewTab: boolean | null;
                  href: string | "#" | null;
                }
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                }
            > | null;
            level?: number;
            _type: "block";
            _key: string;
          }> | null;
          _type: "featureCardIcon";
          _key: string;
        }> | null;
      }
    | {
        _key: string;
        _type: "featuredProducts";
        heading: string | null;
        products?: Array<
          {
            _key: string;
          } & BigcommerceProductReference
        >;
        productHandles: Array<string> | null;
      }
    | {
        _key: string;
        _type: "hero";
        style?: "classic" | "fullBleed";
        contentPosition?: "bottomCenter" | "bottomLeft" | "bottomRight";
        badge?: string;
        title?: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        image: {
          id: string | null;
          preview: string | null;
          alt: string | "untitled";
          hotspot: {
            x: number;
            y: number;
          } | null;
          crop: {
            bottom: number;
            left: number;
            right: number;
            top: number;
          } | null;
        } | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "imageLinkCards";
        eyebrow?: string;
        title: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
        cards: Array<{
          title: string;
          description: string;
          image: {
            id: string | null;
            preview: string | null;
            alt: string | "untitled";
            hotspot: {
              x: number;
              y: number;
            } | null;
            crop: {
              bottom: number;
              left: number;
              right: number;
              top: number;
            } | null;
          } | null;
          url?: CustomUrl;
          _type: "imageLinkCard";
          _key: string;
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "layersShowcase";
        heading: string;
        description: string | null;
        product: BigcommerceProductReference;
        productHandle: string | null;
        productTitle: string | null;
      }
    | {
        _key: string;
        _type: "subscribeNewsletter";
        title?: string;
        subTitle: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        helperText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        image: {
          id: string | null;
          preview: string | null;
          alt: string | "untitled";
          hotspot: {
            x: number;
            y: number;
          } | null;
          crop: {
            bottom: number;
            left: number;
            right: number;
            top: number;
          } | null;
        } | null;
      }
  > | null;
  seoTitle?: string;
  seoDescription?: string;
  seoImage?: SeoImage;
  seoNoIndex?: boolean;
  ogTitle?: string;
  ogDescription?: string;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: querySlugPagePaths
// Query: *[_type == "page" && defined(slug.current)].slug.current
export type QuerySlugPagePathsResult = Array<string>;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryBlogIndexPageData
// Query: *[_type == "blogIndex"][0]{    ...,    _id,    _type,    title,    description,    "displayFeaturedBlogs" : displayFeaturedBlogs == "yes",    "featuredBlogsCount" : featuredBlogsCount,      pageBuilder[]{    ...,    _type,      _type == "collectionBanner" => {    ...,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  }  },      _type == "cta" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },  },      _type == "editorialTwoUp" => {    ...,    "items": array::compact(items[]{      ...,      swatchColor,      "collectionTitle": collection->store.title,      "collectionImage": collection->store.imageUrl,      "collectionHref": select(        defined(collection) => "/collections/" + collection->store.slug.current,        null      ),    })  },      _type == "exploreCategories" => {    ...,      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },    // Picked, else automatic — the shape Featured Products already uses and the    // README already documents. The guard is count() > 0 rather than    // defined(collections), because an editor who adds picks and then removes    // them leaves an empty array behind, and defined([]) is true. On an absent    // field count(null) > 0 evaluates to null, which select() treats as no match    // and falls through — verified against the live dataset, not assumed.    "collections": select(      // A picked category the sync has since tombstoned or the merchant has      // hidden drops out, and if every pick is dead the block renders nothing      // rather than reverting to the automatic row. Same call as the hotspot      // spot-drop and featured-cards.ts: showing an editor four categories they      // did not choose, because the ones they did choose quietly died, is worse      // than showing none.      //      // isVisible is spelled == true rather than != false, and the strictness      // is the point: /collections/[...slug] resolves against live BigCommerce,      // which omits a hidden category from its tree, so a card this block links      // on a null flag is a card pointing at a 404 — the shape featured-cards.ts      // rules out for products. queryAllCollections filters identically for the      // same reason.      //      // array::compact for the other half of the same problem: these references      // are weak, so the target may not exist at all rather than merely being      // tombstoned — which is the normal state of a fresh install between the      // content import and the first sync. The filter does not catch that one.      // A missing document makes store.isDeleted null, null != true is true, so      // the reference passes and the deref then yields null straight into the      // array. featuredProductsBlock compacts the identical shape for the      // identical reason. It stays after the isVisible clause: the clause is      // about what the merchant published, compact is about whether the target      // document exists, and neither answers the other's question.      count(collections) > 0 => array::compact(collections[@->store.isDeleted != true && @->store.isVisible == true]->{  _id,  "title": store.title,  "slug": store.slug.current,  "imageUrl": store.imageUrl,}),      // The parentEntityId clause is what makes "top-level" true rather than      // accidental. Every synced category is flat today (ROB-2566), so it      // filters nothing yet; the moment parentage lands in the sync, without it      // this block starts returning subcategories.      //      // Ordered by title because BigCommerce's own sortOrder reaches the seed      // fixture but is never mapped into the synced document — see      // toCategoryDocument in packages/sanity-sync/src/upsert.ts. Using it would      // need a schema change and a re-sync. Title order is at least a decision;      // the _id order it replaces was an accident of insertion.      *[        _type == "bigcommerceCategory"        && defined(store.slug.current)        && store.isDeleted != true        && store.isVisible == true        && !defined(store.parentEntityId)      ] | order(store.title asc) [0...4]{  _id,  "title": store.title,  "slug": store.slug.current,  "imageUrl": store.imageUrl,}    )  },      _type == "hero" => {    ...,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  },      _type == "faqAccordion" => {    ...,      "faqs": array::compact(faqs[]->{    title,    _id,    _type,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  }),    link{      ...,      "openInNewTab": url.openInNewTab,      "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    )    }  },      _type == "faqCategories" => {    ...,    categories[]{      _key,      title,        "faqs": array::compact(faqs[]->{    title,    _id,    _type,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  })    }  },      _type == "featureCardsIcon" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },    "cards": array::compact(cards[]{      ...,        richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },    })  },      _type == "featuredProducts" => {    ...,    heading,    "productHandles": array::compact(products[]->store.slug.current)  },      _type == "layersShowcase" => {    ...,    heading,    description,    "productHandle": product->store.slug.current,    "productTitle": product->store.title,  },      _type == "subscribeNewsletter" => {    ...,    "subTitle": subTitle[]{      ...,        markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }    },    "helperText": helperText[]{      ...,        markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }    },      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  }  },      _type == "imageLinkCards" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },    "cards": array::compact(cards[]{      ...,      "openInNewTab": url.openInNewTab,      "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),        image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },    })  }  },    "slug": slug.current  }
export type QueryBlogIndexPageDataResult = {
  _id: string;
  _type: "blogIndex";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title: string | null;
  description: string | null;
  slug: string;
  displayFeaturedBlogs: false | true;
  featuredBlogsCount: "1" | "2" | "3" | null;
  pageBuilder: Array<
    | {
        _key: string;
        _type: "collectionBanner";
        eyebrow?: string;
        title: string;
        description?: string;
        image: {
          id: string | null;
          preview: string | null;
          alt: string | "untitled";
          hotspot: {
            x: number;
            y: number;
          } | null;
          crop: {
            bottom: number;
            left: number;
            right: number;
            top: number;
          } | null;
        } | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "cta";
        eyebrow?: string;
        title?: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "editorialTwoUp";
        items: Array<{
          swatchColor: string | null;
          collection: BigcommerceCategoryReference;
          _type: "editorialItem";
          _key: string;
          collectionTitle: string | null;
          collectionImage: string | null;
          collectionHref: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "exploreCategories";
        title: string;
        collections: Array<{
          _id: string;
          title: string | null;
          slug: string | null;
          imageUrl: string | null;
        }> | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "faqAccordion";
        eyebrow?: string;
        title: string;
        subtitle?: string;
        link: {
          title?: string;
          description?: string;
          url?: CustomUrl;
          openInNewTab: boolean | null;
          href: string | null;
        } | null;
        faqs: Array<{
          title: string;
          _id: string;
          _type: "faq";
          richText: Array<{
            children?: Array<{
              marks?: Array<string>;
              text?: string;
              _type: "span";
              _key: string;
            }>;
            style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
            listItem?: "bullet" | "number";
            markDefs: Array<
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                  openInNewTab: boolean | null;
                  href: string | "#" | null;
                }
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                }
            > | null;
            level?: number;
            _type: "block";
            _key: string;
          }> | null;
        }>;
      }
    | {
        _key: string;
        _type: "faqCategories";
        title: string;
        categories: Array<{
          _key: string;
          title: string;
          faqs: Array<{
            title: string;
            _id: string;
            _type: "faq";
            richText: Array<{
              children?: Array<{
                marks?: Array<string>;
                text?: string;
                _type: "span";
                _key: string;
              }>;
              style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
              listItem?: "bullet" | "number";
              markDefs: Array<
                | {
                    customLink?: CustomUrl;
                    _type: "customLink";
                    _key: string;
                    openInNewTab: boolean | null;
                    href: string | "#" | null;
                  }
                | {
                    customLink?: CustomUrl;
                    _type: "customLink";
                    _key: string;
                  }
              > | null;
              level?: number;
              _type: "block";
              _key: string;
            }> | null;
          }>;
        }>;
      }
    | {
        _key: string;
        _type: "featureCardsIcon";
        eyebrow?: string;
        title?: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        cards: Array<{
          icon?: LucideIcon;
          title?: string;
          richText: Array<{
            children?: Array<{
              marks?: Array<string>;
              text?: string;
              _type: "span";
              _key: string;
            }>;
            style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
            listItem?: "bullet" | "number";
            markDefs: Array<
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                  openInNewTab: boolean | null;
                  href: string | "#" | null;
                }
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                }
            > | null;
            level?: number;
            _type: "block";
            _key: string;
          }> | null;
          _type: "featureCardIcon";
          _key: string;
        }> | null;
      }
    | {
        _key: string;
        _type: "featuredProducts";
        heading: string | null;
        products?: Array<
          {
            _key: string;
          } & BigcommerceProductReference
        >;
        productHandles: Array<string> | null;
      }
    | {
        _key: string;
        _type: "hero";
        style?: "classic" | "fullBleed";
        contentPosition?: "bottomCenter" | "bottomLeft" | "bottomRight";
        badge?: string;
        title?: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        image: {
          id: string | null;
          preview: string | null;
          alt: string | "untitled";
          hotspot: {
            x: number;
            y: number;
          } | null;
          crop: {
            bottom: number;
            left: number;
            right: number;
            top: number;
          } | null;
        } | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "imageLinkCards";
        eyebrow?: string;
        title: string;
        richText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        buttons: Array<{
          text: string | null;
          variant: "default" | "link" | "outline" | "secondary" | null;
          _key: string;
          _type: "button";
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
        cards: Array<{
          title: string;
          description: string;
          image: {
            id: string | null;
            preview: string | null;
            alt: string | "untitled";
            hotspot: {
              x: number;
              y: number;
            } | null;
            crop: {
              bottom: number;
              left: number;
              right: number;
              top: number;
            } | null;
          } | null;
          url?: CustomUrl;
          _type: "imageLinkCard";
          _key: string;
          openInNewTab: boolean | null;
          href: string | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "layersShowcase";
        heading: string;
        description: string | null;
        product: BigcommerceProductReference;
        productHandle: string | null;
        productTitle: string | null;
      }
    | {
        _key: string;
        _type: "subscribeNewsletter";
        title?: string;
        subTitle: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        helperText: Array<{
          children?: Array<{
            marks?: Array<string>;
            text?: string;
            _type: "span";
            _key: string;
          }>;
          style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
          listItem?: "bullet" | "number";
          markDefs: Array<
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
                openInNewTab: boolean | null;
                href: string | "#" | null;
              }
            | {
                customLink?: CustomUrl;
                _type: "customLink";
                _key: string;
              }
          > | null;
          level?: number;
          _type: "block";
          _key: string;
        }> | null;
        image: {
          id: string | null;
          preview: string | null;
          alt: string | "untitled";
          hotspot: {
            x: number;
            y: number;
          } | null;
          crop: {
            bottom: number;
            left: number;
            right: number;
            top: number;
          } | null;
        } | null;
      }
  > | null;
  seoTitle?: string;
  seoDescription?: string;
  seoImage?: SeoImage;
  ogTitle?: string;
  ogDescription?: string;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryBlogIndexPageBlogs
// Query: *[_type == "blog" && (seoHideFromLists != true) && ($category == "" || category->slug.current == $category)] | order(orderRank asc) [$start...$end]{      _type,  _id,  title,  description,  "slug":slug.current,  orderRank,    image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },  publishedAt,  "category": category->{ _id, title, "slug": slug.current },    authors[0]->{    _id,    name,    position,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  }  }  }
export type QueryBlogIndexPageBlogsResult = Array<{
  _type: "blog";
  _id: string;
  title: string;
  description: string | null;
  slug: string;
  orderRank: string | null;
  image: {
    id: string | null;
    preview: string | null;
    alt: string | "untitled";
    hotspot: {
      x: number;
      y: number;
    } | null;
    crop: {
      bottom: number;
      left: number;
      right: number;
      top: number;
    } | null;
  };
  publishedAt: string | null;
  category: {
    _id: string;
    title: string;
    slug: string;
  } | null;
  authors: {
    _id: string;
    name: string;
    position: string | null;
    image: {
      id: string | null;
      preview: string | null;
      alt: string | "untitled";
      hotspot: {
        x: number;
        y: number;
      } | null;
      crop: {
        bottom: number;
        left: number;
        right: number;
        top: number;
      } | null;
    } | null;
  } | null;
}>;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryAllBlogDataForSearch
// Query: *[_type == "blog" && defined(slug.current) && (seoHideFromLists != true)]{      _type,  _id,  title,  description,  "slug":slug.current,  orderRank,    image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },  publishedAt,  "category": category->{ _id, title, "slug": slug.current },    authors[0]->{    _id,    name,    position,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  }  }  }
export type QueryAllBlogDataForSearchResult = Array<{
  _type: "blog";
  _id: string;
  title: string;
  description: string | null;
  slug: string;
  orderRank: string | null;
  image: {
    id: string | null;
    preview: string | null;
    alt: string | "untitled";
    hotspot: {
      x: number;
      y: number;
    } | null;
    crop: {
      bottom: number;
      left: number;
      right: number;
      top: number;
    } | null;
  };
  publishedAt: string | null;
  category: {
    _id: string;
    title: string;
    slug: string;
  } | null;
  authors: {
    _id: string;
    name: string;
    position: string | null;
    image: {
      id: string | null;
      preview: string | null;
      alt: string | "untitled";
      hotspot: {
        x: number;
        y: number;
      } | null;
      crop: {
        bottom: number;
        left: number;
        right: number;
        top: number;
      } | null;
    } | null;
  } | null;
}>;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryBlogIndexPageBlogsCount
// Query: count(*[_type == "blog" && (seoHideFromLists != true) && ($category == "" || category->slug.current == $category)])
export type QueryBlogIndexPageBlogsCountResult = number;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryBlogCategories
// Query: *[_type == "category"] | order(orderRank asc){    _id,    title,    "slug": slug.current  }
export type QueryBlogCategoriesResult = Array<{
  _id: string;
  title: string;
  slug: string;
}>;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryBlogSlugPageData
// Query: *[_type == "blog" && slug.current == $slug][0]{    ...,    "slug": slug.current,    "category": category->{ _id, title, "slug": slug.current },      authors[0]->{    _id,    name,    position,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  }  },      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },      pageBuilder[]{    ...,    _type,      _type == "collectionBanner" => {    ...,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  }  },      _type == "cta" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },  },      _type == "editorialTwoUp" => {    ...,    "items": array::compact(items[]{      ...,      swatchColor,      "collectionTitle": collection->store.title,      "collectionImage": collection->store.imageUrl,      "collectionHref": select(        defined(collection) => "/collections/" + collection->store.slug.current,        null      ),    })  },      _type == "exploreCategories" => {    ...,      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },    // Picked, else automatic — the shape Featured Products already uses and the    // README already documents. The guard is count() > 0 rather than    // defined(collections), because an editor who adds picks and then removes    // them leaves an empty array behind, and defined([]) is true. On an absent    // field count(null) > 0 evaluates to null, which select() treats as no match    // and falls through — verified against the live dataset, not assumed.    "collections": select(      // A picked category the sync has since tombstoned or the merchant has      // hidden drops out, and if every pick is dead the block renders nothing      // rather than reverting to the automatic row. Same call as the hotspot      // spot-drop and featured-cards.ts: showing an editor four categories they      // did not choose, because the ones they did choose quietly died, is worse      // than showing none.      //      // isVisible is spelled == true rather than != false, and the strictness      // is the point: /collections/[...slug] resolves against live BigCommerce,      // which omits a hidden category from its tree, so a card this block links      // on a null flag is a card pointing at a 404 — the shape featured-cards.ts      // rules out for products. queryAllCollections filters identically for the      // same reason.      //      // array::compact for the other half of the same problem: these references      // are weak, so the target may not exist at all rather than merely being      // tombstoned — which is the normal state of a fresh install between the      // content import and the first sync. The filter does not catch that one.      // A missing document makes store.isDeleted null, null != true is true, so      // the reference passes and the deref then yields null straight into the      // array. featuredProductsBlock compacts the identical shape for the      // identical reason. It stays after the isVisible clause: the clause is      // about what the merchant published, compact is about whether the target      // document exists, and neither answers the other's question.      count(collections) > 0 => array::compact(collections[@->store.isDeleted != true && @->store.isVisible == true]->{  _id,  "title": store.title,  "slug": store.slug.current,  "imageUrl": store.imageUrl,}),      // The parentEntityId clause is what makes "top-level" true rather than      // accidental. Every synced category is flat today (ROB-2566), so it      // filters nothing yet; the moment parentage lands in the sync, without it      // this block starts returning subcategories.      //      // Ordered by title because BigCommerce's own sortOrder reaches the seed      // fixture but is never mapped into the synced document — see      // toCategoryDocument in packages/sanity-sync/src/upsert.ts. Using it would      // need a schema change and a re-sync. Title order is at least a decision;      // the _id order it replaces was an accident of insertion.      *[        _type == "bigcommerceCategory"        && defined(store.slug.current)        && store.isDeleted != true        && store.isVisible == true        && !defined(store.parentEntityId)      ] | order(store.title asc) [0...4]{  _id,  "title": store.title,  "slug": store.slug.current,  "imageUrl": store.imageUrl,}    )  },      _type == "hero" => {    ...,      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  },      _type == "faqAccordion" => {    ...,      "faqs": array::compact(faqs[]->{    title,    _id,    _type,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  }),    link{      ...,      "openInNewTab": url.openInNewTab,      "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    )    }  },      _type == "faqCategories" => {    ...,    categories[]{      _key,      title,        "faqs": array::compact(faqs[]->{    title,    _id,    _type,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  }  })    }  },      _type == "featureCardsIcon" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },    "cards": array::compact(cards[]{      ...,        richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },    })  },      _type == "featuredProducts" => {    ...,    heading,    "productHandles": array::compact(products[]->store.slug.current)  },      _type == "layersShowcase" => {    ...,    heading,    description,    "productHandle": product->store.slug.current,    "productTitle": product->store.title,  },      _type == "subscribeNewsletter" => {    ...,    "subTitle": subTitle[]{      ...,        markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }    },    "helperText": helperText[]{      ...,        markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }    },      image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  }  },      _type == "imageLinkCards" => {    ...,      richText[]{    ...,      _type == "block" => {    ...,      markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }  },  _type == "image" => {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  },    "caption": caption  },  _type == "imageWithProductHotspots" => {    _type,    _key,    image{  "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }},    showHotspots,      productHotspots[]{    _key,    x,    y,      productWithVariant{    // Null once the sync tombstones the product, which drops the whole spot:    // `ProductHotspotsImage` already bails on a spot with no product. The    // alternative is a card that links to a PDP which 404s, and the repo has    // settled that argument the other way once already — see the comment on the    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,    // so without this the document is still there and still projects happily.    "product": select(      product->store.isDeleted != true => product->{        _id,        "slug": store.slug.current,        store{          title,          previewImageUrl,          // A synced product carries one price, not a min/max range. The          // hotspot card still reads a range, so it is built here rather than          // reshaping the card: a marked-down product keeps the pre-markdown          // figure in `price`, which makes it the top of the range.          "priceRange": {            "minVariantPrice": coalesce(salePrice, price),            "maxVariantPrice": price          }        }      }    ),    variant->{      _id,      store{        title,        price,        imageUrl      }    }  }  }  },  _type == "accordion" => {    _type,    _key,    groups[]{      _key,      title,      body[]{        ...,        _type == "block" => {          ...,            markDefs[]{    ...,      ...customLink{    openInNewTab,    "href": select(      type == "internal" => coalesce(        internal->slug.current,        "/collections/" + internal->store.slug.current      ),      type == "external" => external,      type == "email" => "mailto:" + email,      type == "product" => "/products/" + product->store.slug.current,      "#"    ),  },    _type == "linkInternal" => {      "href": reference->slug.current,    },    _type == "linkExternal" => {      "href": url,      "openInNewTab": newWindow,    },    _type == "linkEmail" => {      "href": "mailto:" + email,    },  }        }      }    }  },  _type == "callout" => {    _type,    _key,    text  }  },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },    "cards": array::compact(cards[]{      ...,      "openInNewTab": url.openInNewTab,      "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),        image {      "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }  },    })  }  }  }
export type QueryBlogSlugPageDataResult = {
  _id: string;
  _type: "blog";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  orderRank?: string;
  title: string;
  description?: string;
  slug: string;
  authors: {
    _id: string;
    name: string;
    position: string | null;
    image: {
      id: string | null;
      preview: string | null;
      alt: string | "untitled";
      hotspot: {
        x: number;
        y: number;
      } | null;
      crop: {
        bottom: number;
        left: number;
        right: number;
        top: number;
      } | null;
    } | null;
  } | null;
  category: {
    _id: string;
    title: string;
    slug: string;
  } | null;
  publishedAt?: string;
  image: {
    id: string | null;
    preview: string | null;
    alt: string | "untitled";
    hotspot: {
      x: number;
      y: number;
    } | null;
    crop: {
      bottom: number;
      left: number;
      right: number;
      top: number;
    } | null;
  };
  richText: Array<
    | {
        _key: string;
        _type: "accordion";
        groups: Array<{
          _key: string;
          title: string;
          body: Array<{
            children?: Array<{
              marks?: Array<string>;
              text?: string;
              _type: "span";
              _key: string;
            }>;
            style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
            listItem?: "bullet" | "number";
            markDefs: Array<
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                  openInNewTab: boolean | null;
                  href: string | "#" | null;
                }
              | {
                  customLink?: CustomUrl;
                  _type: "customLink";
                  _key: string;
                }
            > | null;
            level?: number;
            _type: "block";
            _key: string;
          }> | null;
        }> | null;
      }
    | {
        children?: Array<{
          marks?: Array<string>;
          text?: string;
          _type: "span";
          _key: string;
        }>;
        style?: "h2" | "h3" | "h4" | "h5" | "h6" | "normal";
        listItem?: "bullet" | "number";
        markDefs: Array<
          | {
              customLink?: CustomUrl;
              _type: "customLink";
              _key: string;
              openInNewTab: boolean | null;
              href: string | "#" | null;
            }
          | {
              customLink?: CustomUrl;
              _type: "customLink";
              _key: string;
            }
        > | null;
        level?: number;
        _type: "block";
        _key: string;
      }
    | {
        _key: string;
        _type: "callout";
        text: string;
        link?: Array<
          | ({
              _key: string;
            } & LinkExternal)
          | ({
              _key: string;
            } & LinkInternal)
        >;
      }
    | {
        asset?: SanityImageAssetReference;
        media?: unknown;
        hotspot: {
          x: number;
          y: number;
        } | null;
        crop: {
          bottom: number;
          left: number;
          right: number;
          top: number;
        } | null;
        caption: string | null;
        _type: "image";
        _key: string;
        id: string | null;
        preview: string | null;
        alt: string | "untitled";
      }
    | {
        _key: string;
        _type: "imageWithProductHotspots";
        image: {
          id: string | null;
          preview: string | null;
          alt: string | "untitled";
          hotspot: {
            x: number;
            y: number;
          } | null;
          crop: {
            bottom: number;
            left: number;
            right: number;
            top: number;
          } | null;
        };
        showHotspots: boolean | null;
        productHotspots: Array<{
          _key: string;
          x: number;
          y: number;
          productWithVariant: {
            product: {
              _id: string;
              slug: string | null;
              store: {
                title: string | null;
                previewImageUrl: string | null;
                priceRange: {
                  minVariantPrice: number | null;
                  maxVariantPrice: number | null;
                };
              } | null;
            } | null;
            variant: {
              _id: string;
              store: {
                title: string | null;
                price: number | null;
                imageUrl: string | null;
              } | null;
            } | null;
          } | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "instagram";
        url?: string;
      }
  > | null;
  seoTitle?: string;
  seoDescription?: string;
  seoImage?: SeoImage;
  seoNoIndex?: boolean;
  seoHideFromLists?: boolean;
  ogTitle?: string;
  ogDescription?: string;
  pageBuilder: null;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryBlogPaths
// Query: *[_type == "blog" && defined(slug.current)].slug.current
export type QueryBlogPathsResult = Array<string>;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryHomePageOGData
// Query: *[_type == "homePage" && _id == $id][0]{      _id,  _type,  "title": select(    defined(ogTitle) => ogTitle,    defined(seoTitle) => seoTitle,    title  ),  "description": select(    defined(ogDescription) => ogDescription,    defined(seoDescription) => seoDescription,    description  ),  "image": image.asset->url + "?w=1200&h=630&dpr=2&fit=crop",  "dominantColor": image.asset->metadata.palette.dominant.background,  "seoImage": seoImage.asset->url + "?w=1200&h=630&dpr=2&fit=max",  "logo": *[_type == "settings"][0].logo.asset->url + "?w=80&h=40&dpr=3&fit=max&q=100",  "siteTitle": *[_type == "settings"][0].siteTitle,  "date": coalesce(date, _createdAt)  }
export type QueryHomePageOGDataResult = {
  _id: string;
  _type: "homePage";
  title: string | null;
  description: string | null;
  image: null;
  dominantColor: null;
  seoImage: string | null;
  logo: string | null;
  siteTitle: string | null;
  date: string;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: querySlugPageOGData
// Query: *[_type == "page" && _id == $id][0]{      _id,  _type,  "title": select(    defined(ogTitle) => ogTitle,    defined(seoTitle) => seoTitle,    title  ),  "description": select(    defined(ogDescription) => ogDescription,    defined(seoDescription) => seoDescription,    description  ),  "image": image.asset->url + "?w=1200&h=630&dpr=2&fit=crop",  "dominantColor": image.asset->metadata.palette.dominant.background,  "seoImage": seoImage.asset->url + "?w=1200&h=630&dpr=2&fit=max",  "logo": *[_type == "settings"][0].logo.asset->url + "?w=80&h=40&dpr=3&fit=max&q=100",  "siteTitle": *[_type == "settings"][0].siteTitle,  "date": coalesce(date, _createdAt)  }
export type QuerySlugPageOGDataResult = {
  _id: string;
  _type: "page";
  title: string;
  description: string | null;
  image: string | null;
  dominantColor: string | null;
  seoImage: string | null;
  logo: string | null;
  siteTitle: string | null;
  date: string;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryBlogPageOGData
// Query: *[_type == "blog" && _id == $id][0]{      _id,  _type,  "title": select(    defined(ogTitle) => ogTitle,    defined(seoTitle) => seoTitle,    title  ),  "description": select(    defined(ogDescription) => ogDescription,    defined(seoDescription) => seoDescription,    description  ),  "image": image.asset->url + "?w=1200&h=630&dpr=2&fit=crop",  "dominantColor": image.asset->metadata.palette.dominant.background,  "seoImage": seoImage.asset->url + "?w=1200&h=630&dpr=2&fit=max",  "logo": *[_type == "settings"][0].logo.asset->url + "?w=80&h=40&dpr=3&fit=max&q=100",  "siteTitle": *[_type == "settings"][0].siteTitle,  "date": coalesce(date, _createdAt)  }
export type QueryBlogPageOGDataResult = {
  _id: string;
  _type: "blog";
  title: string;
  description: string | null;
  image: string | null;
  dominantColor: string | null;
  seoImage: string | null;
  logo: string | null;
  siteTitle: string | null;
  date: string;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryGenericPageOGData
// Query: *[ defined(slug.current) && _id == $id][0]{      _id,  _type,  "title": select(    defined(ogTitle) => ogTitle,    defined(seoTitle) => seoTitle,    title  ),  "description": select(    defined(ogDescription) => ogDescription,    defined(seoDescription) => seoDescription,    description  ),  "image": image.asset->url + "?w=1200&h=630&dpr=2&fit=crop",  "dominantColor": image.asset->metadata.palette.dominant.background,  "seoImage": seoImage.asset->url + "?w=1200&h=630&dpr=2&fit=max",  "logo": *[_type == "settings"][0].logo.asset->url + "?w=80&h=40&dpr=3&fit=max&q=100",  "siteTitle": *[_type == "settings"][0].siteTitle,  "date": coalesce(date, _createdAt)  }
export type QueryGenericPageOGDataResult =
  | {
      _id: string;
      _type: "blog";
      title: string;
      description: string | null;
      image: string | null;
      dominantColor: string | null;
      seoImage: string | null;
      logo: string | null;
      siteTitle: string | null;
      date: string;
    }
  | {
      _id: string;
      _type: "blogIndex";
      title: string | null;
      description: string | null;
      image: null;
      dominantColor: null;
      seoImage: string | null;
      logo: string | null;
      siteTitle: string | null;
      date: string;
    }
  | {
      _id: string;
      _type: "category";
      title: string;
      description: string | null;
      image: null;
      dominantColor: null;
      seoImage: null;
      logo: string | null;
      siteTitle: string | null;
      date: string;
    }
  | {
      _id: string;
      _type: "collectionsIndex";
      title: string | null;
      description: string | null;
      image: null;
      dominantColor: null;
      seoImage: string | null;
      logo: string | null;
      siteTitle: string | null;
      date: string;
    }
  | {
      _id: string;
      _type: "homePage";
      title: string | null;
      description: string | null;
      image: null;
      dominantColor: null;
      seoImage: string | null;
      logo: string | null;
      siteTitle: string | null;
      date: string;
    }
  | {
      _id: string;
      _type: "page";
      title: string;
      description: string | null;
      image: string | null;
      dominantColor: string | null;
      seoImage: string | null;
      logo: string | null;
      siteTitle: string | null;
      date: string;
    }
  | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryPromoBannerData
// Query: *[_type == "promoBanner" && _id == "promoBanner"][0]{    _id,    enabled,    text,    "openInNewTab": link.openInNewTab,    "href": select(      link.type == "internal" => coalesce(        link.internal->slug.current,        "/collections/" + link.internal->store.slug.current      ),      link.type == "external" => link.external,      link.type == "email" => "mailto:" + link.email,      link.type == "product" => "/products/" + link.product->store.slug.current,      link.href    ),  }
export type QueryPromoBannerDataResult = {
  _id: "promoBanner";
  enabled: boolean | null;
  text: string;
  openInNewTab: boolean | null;
  href: string | null;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryFooterData
// Query: *[_type == "footer" && _id == "footer"][0]{    _id,    subtitle,    backgroundImage {        "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }    },    columns[]{      _key,      title,      links[]{        _key,        name,        "openInNewTab": url.openInNewTab,        "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),      }    }  }
export type QueryFooterDataResult = {
  _id: "footer";
  subtitle: string | null;
  backgroundImage: {
    id: string | null;
    preview: string | null;
    alt: string | "untitled";
    hotspot: {
      x: number;
      y: number;
    } | null;
    crop: {
      bottom: number;
      left: number;
      right: number;
      top: number;
    } | null;
  } | null;
  columns: Array<{
    _key: string;
    title: string | null;
    links: Array<{
      _key: string;
      name: string | null;
      openInNewTab: boolean | null;
      href: string | null;
    }> | null;
  }> | null;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryNavbarData
// Query: *[_type == "navbar" && _id == "navbar"][0]{    _id,    columns[]{      _key,      _type == "navbarColumn" => {        "type": "column",        title,        links[]{          _key,          name,          icon,          description,          "openInNewTab": url.openInNewTab,          "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    )        }      },      _type == "navbarLink" => {        "type": "link",        name,        description,        "openInNewTab": url.openInNewTab,        "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    )      },      _type == "collectionGroup" => {        "type": "collectionGroup",        title,        "collectionLinks": collectionLinks[]->{          _id,          "slug": store.slug.current,          store{            title,            imageUrl          }        },        "collectionProducts": collectionProducts->{          _id,          "slug": store.slug.current,          store{            title          }        }      }    },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },  }
export type QueryNavbarDataResult = {
  _id: "navbar";
  columns: Array<
    | {
        _key: string;
        type: "collectionGroup";
        title: string;
        collectionLinks: Array<{
          _id: string;
          slug: string | null;
          store: {
            title: string | null;
            imageUrl: string | null;
          } | null;
        }> | null;
        collectionProducts: {
          _id: string;
          slug: string | null;
          store: {
            title: string | null;
          } | null;
        } | null;
      }
    | {
        _key: string;
        type: "link";
        name: string | null;
        description: null;
        openInNewTab: boolean | null;
        href: string | null;
      }
    | {
        _key: string;
        type: "column";
        title: string | null;
        links: Array<{
          _key: string;
          name: string | null;
          icon: LucideIcon | null;
          description: string | null;
          openInNewTab: boolean | null;
          href: string | null;
        }>;
      }
  > | null;
  buttons: Array<{
    text: string | null;
    variant: "default" | "link" | "outline" | "secondary" | null;
    _key: string;
    _type: "button";
    openInNewTab: boolean | null;
    href: string | null;
  }> | null;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: querySitemapData
// Query: {  "page": *[_type == "page" && defined(slug.current)]{    "path": slug.current,    "lastModified": _updatedAt  },  "blog": *[_type == "blog" && defined(slug.current)]{    "path": slug.current,    "lastModified": _updatedAt  }}
export type QuerySitemapDataResult = {
  page: Array<{
    path: string;
    lastModified: string;
  }>;
  blog: Array<{
    path: string;
    lastModified: string;
  }>;
};

// Source: ../../packages/sanity/src/query.ts
// Variable: queryGlobalSeoSettings
// Query: *[_type == "settings"][0]{    _id,    _type,    siteTitle,    logo {        "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }    },    siteDescription,    socialLinks{      linkedin,      facebook,      twitter,      instagram,      youtube    }  }
export type QueryGlobalSeoSettingsResult = {
  _id: string;
  _type: "settings";
  siteTitle: string;
  logo: {
    id: string | null;
    preview: string | null;
    alt: string | "untitled";
    hotspot: {
      x: number;
      y: number;
    } | null;
    crop: {
      bottom: number;
      left: number;
      right: number;
      top: number;
    } | null;
  } | null;
  siteDescription: string;
  socialLinks: {
    linkedin: string | null;
    facebook: string | null;
    twitter: string | null;
    instagram: string | null;
    youtube: string | null;
  } | null;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: querySettingsData
// Query: *[_type == "settings"][0]{    _id,    _type,    siteTitle,    siteDescription,    "logo": logo.asset->url + "?w=80&h=40&dpr=3&fit=max",    "socialLinks": socialLinks,    "contactEmail": contactEmail,  }
export type QuerySettingsDataResult = {
  _id: string;
  _type: "settings";
  siteTitle: string;
  siteDescription: string;
  logo: string | null;
  socialLinks: {
    linkedin?: string;
    facebook?: string;
    twitter?: string;
    instagram?: string;
    youtube?: string;
  } | null;
  contactEmail: string | null;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryRedirects
// Query: *[_type == "redirect" && status == "active" && defined(source.current) && defined(destination.current)]{    "source":source.current,    "destination":destination.current,    "permanent" : permanent == "true"  }
export type QueryRedirectsResult = Array<{
  source: string;
  destination: string;
  permanent: false | true;
}>;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryRedirectBySource
// Query: *[_type == "redirect" && status == "active" && source.current == $source && defined(destination.current)][0]{    "source":source.current,    "destination":destination.current,    "permanent" : permanent == "true"  }
export type QueryRedirectBySourceResult = {
  source: string;
  destination: string;
  permanent: false | true;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryCollectionsIndexPageData
// Query: *[_type == "collectionsIndex"][0]{    ...,    _id,    _type,    title,    subtitle,    heroTitle,    heroImage {        "id": asset._ref,  "preview": asset->metadata.lqip,  "alt": coalesce(    alt,    asset->altText,    caption,    asset->originalFilename,    "untitled"  ),  hotspot {    x,    y  },  crop {    bottom,    left,    right,    top  }    },      buttons[]{    text,    variant,    _key,    _type,    "openInNewTab": url.openInNewTab,    "href": select(      url.type == "internal" => coalesce(        url.internal->slug.current,        "/collections/" + url.internal->store.slug.current      ),      url.type == "external" => url.external,      url.type == "email" => "mailto:" + url.email,      url.type == "product" => "/products/" + url.product->store.slug.current,      url.href    ),  },    "slug": slug.current  }
export type QueryCollectionsIndexPageDataResult = {
  _id: string;
  _type: "collectionsIndex";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title: string | null;
  subtitle: string | null;
  heroTitle: string | null;
  heroImage: {
    id: string | null;
    preview: string | null;
    alt: string | "untitled";
    hotspot: {
      x: number;
      y: number;
    } | null;
    crop: {
      bottom: number;
      left: number;
      right: number;
      top: number;
    } | null;
  } | null;
  buttons: Array<{
    text: string | null;
    variant: "default" | "link" | "outline" | "secondary" | null;
    _key: string;
    _type: "button";
    openInNewTab: boolean | null;
    href: string | null;
  }> | null;
  slug: string;
  seoTitle?: string;
  seoDescription?: string;
  seoImage?: SeoImage;
  ogTitle?: string;
  ogDescription?: string;
} | null;

// Source: ../../packages/sanity/src/query.ts
// Variable: queryAllCollections
// Query: *[_type == "bigcommerceCategory" && defined(store.slug.current) && store.isDeleted != true && store.isVisible == true]{    _id,    _createdAt,    "title": store.title,    "slug": store.slug.current,    "imageUrl": store.imageUrl,    "description": store.descriptionHtml  }
export type QueryAllCollectionsResult = Array<{
  _id: string;
  _createdAt: string;
  title: string | null;
  slug: string | null;
  imageUrl: string | null;
  description: string | null;
}>;

// Query TypeMap
import "@sanity/client";
declare module "@sanity/client" {
  interface SanityQueries {
    '\n  *[_type == "page" && defined(image)][0]{\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n\n  }.image\n': QueryImageTypeResult;
    '*[_type == "homePage" && _id == "homePage"][0]{\n    ...,\n    _id,\n    _type,\n    "slug": slug.current,\n    title,\n    description,\n    \n  pageBuilder[]{\n    ...,\n    _type,\n    \n  _type == "collectionBanner" => {\n    ...,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n\n  }\n,\n    \n  _type == "cta" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n  }\n,\n    \n  _type == "editorialTwoUp" => {\n    ...,\n    "items": array::compact(items[]{\n      ...,\n      swatchColor,\n      "collectionTitle": collection->store.title,\n      "collectionImage": collection->store.imageUrl,\n      "collectionHref": select(\n        defined(collection) => "/collections/" + collection->store.slug.current,\n        null\n      ),\n    })\n  }\n,\n    \n  _type == "exploreCategories" => {\n    ...,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    // Picked, else automatic \u2014 the shape Featured Products already uses and the\n    // README already documents. The guard is count() > 0 rather than\n    // defined(collections), because an editor who adds picks and then removes\n    // them leaves an empty array behind, and defined([]) is true. On an absent\n    // field count(null) > 0 evaluates to null, which select() treats as no match\n    // and falls through \u2014 verified against the live dataset, not assumed.\n    "collections": select(\n      // A picked category the sync has since tombstoned or the merchant has\n      // hidden drops out, and if every pick is dead the block renders nothing\n      // rather than reverting to the automatic row. Same call as the hotspot\n      // spot-drop and featured-cards.ts: showing an editor four categories they\n      // did not choose, because the ones they did choose quietly died, is worse\n      // than showing none.\n      //\n      // isVisible is spelled == true rather than != false, and the strictness\n      // is the point: /collections/[...slug] resolves against live BigCommerce,\n      // which omits a hidden category from its tree, so a card this block links\n      // on a null flag is a card pointing at a 404 \u2014 the shape featured-cards.ts\n      // rules out for products. queryAllCollections filters identically for the\n      // same reason.\n      //\n      // array::compact for the other half of the same problem: these references\n      // are weak, so the target may not exist at all rather than merely being\n      // tombstoned \u2014 which is the normal state of a fresh install between the\n      // content import and the first sync. The filter does not catch that one.\n      // A missing document makes store.isDeleted null, null != true is true, so\n      // the reference passes and the deref then yields null straight into the\n      // array. featuredProductsBlock compacts the identical shape for the\n      // identical reason. It stays after the isVisible clause: the clause is\n      // about what the merchant published, compact is about whether the target\n      // document exists, and neither answers the other\'s question.\n      count(collections) > 0 => array::compact(collections[@->store.isDeleted != true && @->store.isVisible == true]->{\n  _id,\n  "title": store.title,\n  "slug": store.slug.current,\n  "imageUrl": store.imageUrl,\n}),\n      // The parentEntityId clause is what makes "top-level" true rather than\n      // accidental. Every synced category is flat today (ROB-2566), so it\n      // filters nothing yet; the moment parentage lands in the sync, without it\n      // this block starts returning subcategories.\n      //\n      // Ordered by title because BigCommerce\'s own sortOrder reaches the seed\n      // fixture but is never mapped into the synced document \u2014 see\n      // toCategoryDocument in packages/sanity-sync/src/upsert.ts. Using it would\n      // need a schema change and a re-sync. Title order is at least a decision;\n      // the _id order it replaces was an accident of insertion.\n      *[\n        _type == "bigcommerceCategory"\n        && defined(store.slug.current)\n        && store.isDeleted != true\n        && store.isVisible == true\n        && !defined(store.parentEntityId)\n      ] | order(store.title asc) [0...4]{\n  _id,\n  "title": store.title,\n  "slug": store.slug.current,\n  "imageUrl": store.imageUrl,\n}\n    )\n  }\n,\n    \n  _type == "hero" => {\n    ...,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  }\n,\n    \n  _type == "faqAccordion" => {\n    ...,\n    \n  "faqs": array::compact(faqs[]->{\n    title,\n    _id,\n    _type,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  })\n,\n    link{\n      ...,\n      "openInNewTab": url.openInNewTab,\n      "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    )\n    }\n  }\n,\n    \n  _type == "faqCategories" => {\n    ...,\n    categories[]{\n      _key,\n      title,\n      \n  "faqs": array::compact(faqs[]->{\n    title,\n    _id,\n    _type,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  })\n\n    }\n  }\n,\n    \n  _type == "featureCardsIcon" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    "cards": array::compact(cards[]{\n      ...,\n      \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    })\n  }\n,\n    \n  _type == "featuredProducts" => {\n    ...,\n    heading,\n    "productHandles": array::compact(products[]->store.slug.current)\n  }\n,\n    \n  _type == "layersShowcase" => {\n    ...,\n    heading,\n    description,\n    "productHandle": product->store.slug.current,\n    "productTitle": product->store.title,\n  }\n,\n    \n  _type == "subscribeNewsletter" => {\n    ...,\n    "subTitle": subTitle[]{\n      ...,\n      \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n    },\n    "helperText": helperText[]{\n      ...,\n      \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n    },\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n\n  }\n,\n    \n  _type == "imageLinkCards" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    "cards": array::compact(cards[]{\n      ...,\n      "openInNewTab": url.openInNewTab,\n      "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n      \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    })\n  }\n\n  }\n\n  }': QueryHomePageDataResult;
    '\n  *[_type == "page" && slug.current == $slug][0]{\n    ...,\n    "slug": slug.current,\n    \n  pageBuilder[]{\n    ...,\n    _type,\n    \n  _type == "collectionBanner" => {\n    ...,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n\n  }\n,\n    \n  _type == "cta" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n  }\n,\n    \n  _type == "editorialTwoUp" => {\n    ...,\n    "items": array::compact(items[]{\n      ...,\n      swatchColor,\n      "collectionTitle": collection->store.title,\n      "collectionImage": collection->store.imageUrl,\n      "collectionHref": select(\n        defined(collection) => "/collections/" + collection->store.slug.current,\n        null\n      ),\n    })\n  }\n,\n    \n  _type == "exploreCategories" => {\n    ...,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    // Picked, else automatic \u2014 the shape Featured Products already uses and the\n    // README already documents. The guard is count() > 0 rather than\n    // defined(collections), because an editor who adds picks and then removes\n    // them leaves an empty array behind, and defined([]) is true. On an absent\n    // field count(null) > 0 evaluates to null, which select() treats as no match\n    // and falls through \u2014 verified against the live dataset, not assumed.\n    "collections": select(\n      // A picked category the sync has since tombstoned or the merchant has\n      // hidden drops out, and if every pick is dead the block renders nothing\n      // rather than reverting to the automatic row. Same call as the hotspot\n      // spot-drop and featured-cards.ts: showing an editor four categories they\n      // did not choose, because the ones they did choose quietly died, is worse\n      // than showing none.\n      //\n      // isVisible is spelled == true rather than != false, and the strictness\n      // is the point: /collections/[...slug] resolves against live BigCommerce,\n      // which omits a hidden category from its tree, so a card this block links\n      // on a null flag is a card pointing at a 404 \u2014 the shape featured-cards.ts\n      // rules out for products. queryAllCollections filters identically for the\n      // same reason.\n      //\n      // array::compact for the other half of the same problem: these references\n      // are weak, so the target may not exist at all rather than merely being\n      // tombstoned \u2014 which is the normal state of a fresh install between the\n      // content import and the first sync. The filter does not catch that one.\n      // A missing document makes store.isDeleted null, null != true is true, so\n      // the reference passes and the deref then yields null straight into the\n      // array. featuredProductsBlock compacts the identical shape for the\n      // identical reason. It stays after the isVisible clause: the clause is\n      // about what the merchant published, compact is about whether the target\n      // document exists, and neither answers the other\'s question.\n      count(collections) > 0 => array::compact(collections[@->store.isDeleted != true && @->store.isVisible == true]->{\n  _id,\n  "title": store.title,\n  "slug": store.slug.current,\n  "imageUrl": store.imageUrl,\n}),\n      // The parentEntityId clause is what makes "top-level" true rather than\n      // accidental. Every synced category is flat today (ROB-2566), so it\n      // filters nothing yet; the moment parentage lands in the sync, without it\n      // this block starts returning subcategories.\n      //\n      // Ordered by title because BigCommerce\'s own sortOrder reaches the seed\n      // fixture but is never mapped into the synced document \u2014 see\n      // toCategoryDocument in packages/sanity-sync/src/upsert.ts. Using it would\n      // need a schema change and a re-sync. Title order is at least a decision;\n      // the _id order it replaces was an accident of insertion.\n      *[\n        _type == "bigcommerceCategory"\n        && defined(store.slug.current)\n        && store.isDeleted != true\n        && store.isVisible == true\n        && !defined(store.parentEntityId)\n      ] | order(store.title asc) [0...4]{\n  _id,\n  "title": store.title,\n  "slug": store.slug.current,\n  "imageUrl": store.imageUrl,\n}\n    )\n  }\n,\n    \n  _type == "hero" => {\n    ...,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  }\n,\n    \n  _type == "faqAccordion" => {\n    ...,\n    \n  "faqs": array::compact(faqs[]->{\n    title,\n    _id,\n    _type,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  })\n,\n    link{\n      ...,\n      "openInNewTab": url.openInNewTab,\n      "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    )\n    }\n  }\n,\n    \n  _type == "faqCategories" => {\n    ...,\n    categories[]{\n      _key,\n      title,\n      \n  "faqs": array::compact(faqs[]->{\n    title,\n    _id,\n    _type,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  })\n\n    }\n  }\n,\n    \n  _type == "featureCardsIcon" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    "cards": array::compact(cards[]{\n      ...,\n      \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    })\n  }\n,\n    \n  _type == "featuredProducts" => {\n    ...,\n    heading,\n    "productHandles": array::compact(products[]->store.slug.current)\n  }\n,\n    \n  _type == "layersShowcase" => {\n    ...,\n    heading,\n    description,\n    "productHandle": product->store.slug.current,\n    "productTitle": product->store.title,\n  }\n,\n    \n  _type == "subscribeNewsletter" => {\n    ...,\n    "subTitle": subTitle[]{\n      ...,\n      \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n    },\n    "helperText": helperText[]{\n      ...,\n      \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n    },\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n\n  }\n,\n    \n  _type == "imageLinkCards" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    "cards": array::compact(cards[]{\n      ...,\n      "openInNewTab": url.openInNewTab,\n      "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n      \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    })\n  }\n\n  }\n\n  }\n  ': QuerySlugPageDataResult;
    '\n  *[_type == "page" && defined(slug.current)].slug.current\n': QuerySlugPagePathsResult;
    '\n  *[_type == "blogIndex"][0]{\n    ...,\n    _id,\n    _type,\n    title,\n    description,\n    "displayFeaturedBlogs" : displayFeaturedBlogs == "yes",\n    "featuredBlogsCount" : featuredBlogsCount,\n    \n  pageBuilder[]{\n    ...,\n    _type,\n    \n  _type == "collectionBanner" => {\n    ...,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n\n  }\n,\n    \n  _type == "cta" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n  }\n,\n    \n  _type == "editorialTwoUp" => {\n    ...,\n    "items": array::compact(items[]{\n      ...,\n      swatchColor,\n      "collectionTitle": collection->store.title,\n      "collectionImage": collection->store.imageUrl,\n      "collectionHref": select(\n        defined(collection) => "/collections/" + collection->store.slug.current,\n        null\n      ),\n    })\n  }\n,\n    \n  _type == "exploreCategories" => {\n    ...,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    // Picked, else automatic \u2014 the shape Featured Products already uses and the\n    // README already documents. The guard is count() > 0 rather than\n    // defined(collections), because an editor who adds picks and then removes\n    // them leaves an empty array behind, and defined([]) is true. On an absent\n    // field count(null) > 0 evaluates to null, which select() treats as no match\n    // and falls through \u2014 verified against the live dataset, not assumed.\n    "collections": select(\n      // A picked category the sync has since tombstoned or the merchant has\n      // hidden drops out, and if every pick is dead the block renders nothing\n      // rather than reverting to the automatic row. Same call as the hotspot\n      // spot-drop and featured-cards.ts: showing an editor four categories they\n      // did not choose, because the ones they did choose quietly died, is worse\n      // than showing none.\n      //\n      // isVisible is spelled == true rather than != false, and the strictness\n      // is the point: /collections/[...slug] resolves against live BigCommerce,\n      // which omits a hidden category from its tree, so a card this block links\n      // on a null flag is a card pointing at a 404 \u2014 the shape featured-cards.ts\n      // rules out for products. queryAllCollections filters identically for the\n      // same reason.\n      //\n      // array::compact for the other half of the same problem: these references\n      // are weak, so the target may not exist at all rather than merely being\n      // tombstoned \u2014 which is the normal state of a fresh install between the\n      // content import and the first sync. The filter does not catch that one.\n      // A missing document makes store.isDeleted null, null != true is true, so\n      // the reference passes and the deref then yields null straight into the\n      // array. featuredProductsBlock compacts the identical shape for the\n      // identical reason. It stays after the isVisible clause: the clause is\n      // about what the merchant published, compact is about whether the target\n      // document exists, and neither answers the other\'s question.\n      count(collections) > 0 => array::compact(collections[@->store.isDeleted != true && @->store.isVisible == true]->{\n  _id,\n  "title": store.title,\n  "slug": store.slug.current,\n  "imageUrl": store.imageUrl,\n}),\n      // The parentEntityId clause is what makes "top-level" true rather than\n      // accidental. Every synced category is flat today (ROB-2566), so it\n      // filters nothing yet; the moment parentage lands in the sync, without it\n      // this block starts returning subcategories.\n      //\n      // Ordered by title because BigCommerce\'s own sortOrder reaches the seed\n      // fixture but is never mapped into the synced document \u2014 see\n      // toCategoryDocument in packages/sanity-sync/src/upsert.ts. Using it would\n      // need a schema change and a re-sync. Title order is at least a decision;\n      // the _id order it replaces was an accident of insertion.\n      *[\n        _type == "bigcommerceCategory"\n        && defined(store.slug.current)\n        && store.isDeleted != true\n        && store.isVisible == true\n        && !defined(store.parentEntityId)\n      ] | order(store.title asc) [0...4]{\n  _id,\n  "title": store.title,\n  "slug": store.slug.current,\n  "imageUrl": store.imageUrl,\n}\n    )\n  }\n,\n    \n  _type == "hero" => {\n    ...,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  }\n,\n    \n  _type == "faqAccordion" => {\n    ...,\n    \n  "faqs": array::compact(faqs[]->{\n    title,\n    _id,\n    _type,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  })\n,\n    link{\n      ...,\n      "openInNewTab": url.openInNewTab,\n      "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    )\n    }\n  }\n,\n    \n  _type == "faqCategories" => {\n    ...,\n    categories[]{\n      _key,\n      title,\n      \n  "faqs": array::compact(faqs[]->{\n    title,\n    _id,\n    _type,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  })\n\n    }\n  }\n,\n    \n  _type == "featureCardsIcon" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    "cards": array::compact(cards[]{\n      ...,\n      \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    })\n  }\n,\n    \n  _type == "featuredProducts" => {\n    ...,\n    heading,\n    "productHandles": array::compact(products[]->store.slug.current)\n  }\n,\n    \n  _type == "layersShowcase" => {\n    ...,\n    heading,\n    description,\n    "productHandle": product->store.slug.current,\n    "productTitle": product->store.title,\n  }\n,\n    \n  _type == "subscribeNewsletter" => {\n    ...,\n    "subTitle": subTitle[]{\n      ...,\n      \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n    },\n    "helperText": helperText[]{\n      ...,\n      \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n    },\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n\n  }\n,\n    \n  _type == "imageLinkCards" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    "cards": array::compact(cards[]{\n      ...,\n      "openInNewTab": url.openInNewTab,\n      "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n      \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    })\n  }\n\n  }\n,\n    "slug": slug.current\n  }\n': QueryBlogIndexPageDataResult;
    '\n  *[_type == "blog" && (seoHideFromLists != true) && ($category == "" || category->slug.current == $category)] | order(orderRank asc) [$start...$end]{\n    \n  _type,\n  _id,\n  title,\n  description,\n  "slug":slug.current,\n  orderRank,\n  \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n  publishedAt,\n  "category": category->{ _id, title, "slug": slug.current },\n  \n  authors[0]->{\n    _id,\n    name,\n    position,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n\n  }\n\n\n  }\n': QueryBlogIndexPageBlogsResult;
    '\n  *[_type == "blog" && defined(slug.current) && (seoHideFromLists != true)]{\n    \n  _type,\n  _id,\n  title,\n  description,\n  "slug":slug.current,\n  orderRank,\n  \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n  publishedAt,\n  "category": category->{ _id, title, "slug": slug.current },\n  \n  authors[0]->{\n    _id,\n    name,\n    position,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n\n  }\n\n\n  }\n': QueryAllBlogDataForSearchResult;
    '\n  count(*[_type == "blog" && (seoHideFromLists != true) && ($category == "" || category->slug.current == $category)])\n': QueryBlogIndexPageBlogsCountResult;
    '\n  *[_type == "category"] | order(orderRank asc){\n    _id,\n    title,\n    "slug": slug.current\n  }\n': QueryBlogCategoriesResult;
    '\n  *[_type == "blog" && slug.current == $slug][0]{\n    ...,\n    "slug": slug.current,\n    "category": category->{ _id, title, "slug": slug.current },\n    \n  authors[0]->{\n    _id,\n    name,\n    position,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n\n  }\n,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    \n  pageBuilder[]{\n    ...,\n    _type,\n    \n  _type == "collectionBanner" => {\n    ...,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n\n  }\n,\n    \n  _type == "cta" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n  }\n,\n    \n  _type == "editorialTwoUp" => {\n    ...,\n    "items": array::compact(items[]{\n      ...,\n      swatchColor,\n      "collectionTitle": collection->store.title,\n      "collectionImage": collection->store.imageUrl,\n      "collectionHref": select(\n        defined(collection) => "/collections/" + collection->store.slug.current,\n        null\n      ),\n    })\n  }\n,\n    \n  _type == "exploreCategories" => {\n    ...,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    // Picked, else automatic \u2014 the shape Featured Products already uses and the\n    // README already documents. The guard is count() > 0 rather than\n    // defined(collections), because an editor who adds picks and then removes\n    // them leaves an empty array behind, and defined([]) is true. On an absent\n    // field count(null) > 0 evaluates to null, which select() treats as no match\n    // and falls through \u2014 verified against the live dataset, not assumed.\n    "collections": select(\n      // A picked category the sync has since tombstoned or the merchant has\n      // hidden drops out, and if every pick is dead the block renders nothing\n      // rather than reverting to the automatic row. Same call as the hotspot\n      // spot-drop and featured-cards.ts: showing an editor four categories they\n      // did not choose, because the ones they did choose quietly died, is worse\n      // than showing none.\n      //\n      // isVisible is spelled == true rather than != false, and the strictness\n      // is the point: /collections/[...slug] resolves against live BigCommerce,\n      // which omits a hidden category from its tree, so a card this block links\n      // on a null flag is a card pointing at a 404 \u2014 the shape featured-cards.ts\n      // rules out for products. queryAllCollections filters identically for the\n      // same reason.\n      //\n      // array::compact for the other half of the same problem: these references\n      // are weak, so the target may not exist at all rather than merely being\n      // tombstoned \u2014 which is the normal state of a fresh install between the\n      // content import and the first sync. The filter does not catch that one.\n      // A missing document makes store.isDeleted null, null != true is true, so\n      // the reference passes and the deref then yields null straight into the\n      // array. featuredProductsBlock compacts the identical shape for the\n      // identical reason. It stays after the isVisible clause: the clause is\n      // about what the merchant published, compact is about whether the target\n      // document exists, and neither answers the other\'s question.\n      count(collections) > 0 => array::compact(collections[@->store.isDeleted != true && @->store.isVisible == true]->{\n  _id,\n  "title": store.title,\n  "slug": store.slug.current,\n  "imageUrl": store.imageUrl,\n}),\n      // The parentEntityId clause is what makes "top-level" true rather than\n      // accidental. Every synced category is flat today (ROB-2566), so it\n      // filters nothing yet; the moment parentage lands in the sync, without it\n      // this block starts returning subcategories.\n      //\n      // Ordered by title because BigCommerce\'s own sortOrder reaches the seed\n      // fixture but is never mapped into the synced document \u2014 see\n      // toCategoryDocument in packages/sanity-sync/src/upsert.ts. Using it would\n      // need a schema change and a re-sync. Title order is at least a decision;\n      // the _id order it replaces was an accident of insertion.\n      *[\n        _type == "bigcommerceCategory"\n        && defined(store.slug.current)\n        && store.isDeleted != true\n        && store.isVisible == true\n        && !defined(store.parentEntityId)\n      ] | order(store.title asc) [0...4]{\n  _id,\n  "title": store.title,\n  "slug": store.slug.current,\n  "imageUrl": store.imageUrl,\n}\n    )\n  }\n,\n    \n  _type == "hero" => {\n    ...,\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  }\n,\n    \n  _type == "faqAccordion" => {\n    ...,\n    \n  "faqs": array::compact(faqs[]->{\n    title,\n    _id,\n    _type,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  })\n,\n    link{\n      ...,\n      "openInNewTab": url.openInNewTab,\n      "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    )\n    }\n  }\n,\n    \n  _type == "faqCategories" => {\n    ...,\n    categories[]{\n      _key,\n      title,\n      \n  "faqs": array::compact(faqs[]->{\n    title,\n    _id,\n    _type,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n\n  })\n\n    }\n  }\n,\n    \n  _type == "featureCardsIcon" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    "cards": array::compact(cards[]{\n      ...,\n      \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    })\n  }\n,\n    \n  _type == "featuredProducts" => {\n    ...,\n    heading,\n    "productHandles": array::compact(products[]->store.slug.current)\n  }\n,\n    \n  _type == "layersShowcase" => {\n    ...,\n    heading,\n    description,\n    "productHandle": product->store.slug.current,\n    "productTitle": product->store.title,\n  }\n,\n    \n  _type == "subscribeNewsletter" => {\n    ...,\n    "subTitle": subTitle[]{\n      ...,\n      \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n    },\n    "helperText": helperText[]{\n      ...,\n      \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n    },\n    \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n\n  }\n,\n    \n  _type == "imageLinkCards" => {\n    ...,\n    \n  richText[]{\n    ...,\n    \n  _type == "block" => {\n    ...,\n    \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n  },\n  _type == "image" => {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n,\n    "caption": caption\n  },\n  _type == "imageWithProductHotspots" => {\n    _type,\n    _key,\n    image{\n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n},\n    showHotspots,\n    \n  productHotspots[]{\n    _key,\n    x,\n    y,\n    \n  productWithVariant{\n    // Null once the sync tombstones the product, which drops the whole spot:\n    // `ProductHotspotsImage` already bails on a spot with no product. The\n    // alternative is a card that links to a PDP which 404s, and the repo has\n    // settled that argument the other way once already \u2014 see the comment on the\n    // flatMap in `components/product/featured-cards.ts`. The sync soft-deletes,\n    // so without this the document is still there and still projects happily.\n    "product": select(\n      product->store.isDeleted != true => product->{\n        _id,\n        "slug": store.slug.current,\n        store{\n          title,\n          previewImageUrl,\n          // A synced product carries one price, not a min/max range. The\n          // hotspot card still reads a range, so it is built here rather than\n          // reshaping the card: a marked-down product keeps the pre-markdown\n          // figure in `price`, which makes it the top of the range.\n          "priceRange": {\n            "minVariantPrice": coalesce(salePrice, price),\n            "maxVariantPrice": price\n          }\n        }\n      }\n    ),\n    variant->{\n      _id,\n      store{\n        title,\n        price,\n        imageUrl\n      }\n    }\n  }\n\n  }\n\n  },\n  _type == "accordion" => {\n    _type,\n    _key,\n    groups[]{\n      _key,\n      title,\n      body[]{\n        ...,\n        _type == "block" => {\n          ...,\n          \n  markDefs[]{\n    ...,\n    \n  ...customLink{\n    openInNewTab,\n    "href": select(\n      type == "internal" => coalesce(\n        internal->slug.current,\n        "/collections/" + internal->store.slug.current\n      ),\n      type == "external" => external,\n      type == "email" => "mailto:" + email,\n      type == "product" => "/products/" + product->store.slug.current,\n      "#"\n    ),\n  }\n,\n    _type == "linkInternal" => {\n      "href": reference->slug.current,\n    },\n    _type == "linkExternal" => {\n      "href": url,\n      "openInNewTab": newWindow,\n    },\n    _type == "linkEmail" => {\n      "href": "mailto:" + email,\n    },\n  }\n\n        }\n      }\n    }\n  },\n  _type == "callout" => {\n    _type,\n    _key,\n    text\n  }\n\n  }\n,\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    "cards": array::compact(cards[]{\n      ...,\n      "openInNewTab": url.openInNewTab,\n      "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n      \n  image {\n    \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n  }\n,\n    })\n  }\n\n  }\n\n  }\n': QueryBlogSlugPageDataResult;
    '\n  *[_type == "blog" && defined(slug.current)].slug.current\n': QueryBlogPathsResult;
    '\n  *[_type == "homePage" && _id == $id][0]{\n    \n  _id,\n  _type,\n  "title": select(\n    defined(ogTitle) => ogTitle,\n    defined(seoTitle) => seoTitle,\n    title\n  ),\n  "description": select(\n    defined(ogDescription) => ogDescription,\n    defined(seoDescription) => seoDescription,\n    description\n  ),\n  "image": image.asset->url + "?w=1200&h=630&dpr=2&fit=crop",\n  "dominantColor": image.asset->metadata.palette.dominant.background,\n  "seoImage": seoImage.asset->url + "?w=1200&h=630&dpr=2&fit=max",\n  "logo": *[_type == "settings"][0].logo.asset->url + "?w=80&h=40&dpr=3&fit=max&q=100",\n  "siteTitle": *[_type == "settings"][0].siteTitle,\n  "date": coalesce(date, _createdAt)\n\n  }\n  ': QueryHomePageOGDataResult;
    '\n  *[_type == "page" && _id == $id][0]{\n    \n  _id,\n  _type,\n  "title": select(\n    defined(ogTitle) => ogTitle,\n    defined(seoTitle) => seoTitle,\n    title\n  ),\n  "description": select(\n    defined(ogDescription) => ogDescription,\n    defined(seoDescription) => seoDescription,\n    description\n  ),\n  "image": image.asset->url + "?w=1200&h=630&dpr=2&fit=crop",\n  "dominantColor": image.asset->metadata.palette.dominant.background,\n  "seoImage": seoImage.asset->url + "?w=1200&h=630&dpr=2&fit=max",\n  "logo": *[_type == "settings"][0].logo.asset->url + "?w=80&h=40&dpr=3&fit=max&q=100",\n  "siteTitle": *[_type == "settings"][0].siteTitle,\n  "date": coalesce(date, _createdAt)\n\n  }\n': QuerySlugPageOGDataResult;
    '\n  *[_type == "blog" && _id == $id][0]{\n    \n  _id,\n  _type,\n  "title": select(\n    defined(ogTitle) => ogTitle,\n    defined(seoTitle) => seoTitle,\n    title\n  ),\n  "description": select(\n    defined(ogDescription) => ogDescription,\n    defined(seoDescription) => seoDescription,\n    description\n  ),\n  "image": image.asset->url + "?w=1200&h=630&dpr=2&fit=crop",\n  "dominantColor": image.asset->metadata.palette.dominant.background,\n  "seoImage": seoImage.asset->url + "?w=1200&h=630&dpr=2&fit=max",\n  "logo": *[_type == "settings"][0].logo.asset->url + "?w=80&h=40&dpr=3&fit=max&q=100",\n  "siteTitle": *[_type == "settings"][0].siteTitle,\n  "date": coalesce(date, _createdAt)\n\n  }\n': QueryBlogPageOGDataResult;
    '\n  *[ defined(slug.current) && _id == $id][0]{\n    \n  _id,\n  _type,\n  "title": select(\n    defined(ogTitle) => ogTitle,\n    defined(seoTitle) => seoTitle,\n    title\n  ),\n  "description": select(\n    defined(ogDescription) => ogDescription,\n    defined(seoDescription) => seoDescription,\n    description\n  ),\n  "image": image.asset->url + "?w=1200&h=630&dpr=2&fit=crop",\n  "dominantColor": image.asset->metadata.palette.dominant.background,\n  "seoImage": seoImage.asset->url + "?w=1200&h=630&dpr=2&fit=max",\n  "logo": *[_type == "settings"][0].logo.asset->url + "?w=80&h=40&dpr=3&fit=max&q=100",\n  "siteTitle": *[_type == "settings"][0].siteTitle,\n  "date": coalesce(date, _createdAt)\n\n  }\n': QueryGenericPageOGDataResult;
    '\n  *[_type == "promoBanner" && _id == "promoBanner"][0]{\n    _id,\n    enabled,\n    text,\n    "openInNewTab": link.openInNewTab,\n    "href": select(\n      link.type == "internal" => coalesce(\n        link.internal->slug.current,\n        "/collections/" + link.internal->store.slug.current\n      ),\n      link.type == "external" => link.external,\n      link.type == "email" => "mailto:" + link.email,\n      link.type == "product" => "/products/" + link.product->store.slug.current,\n      link.href\n    ),\n  }\n': QueryPromoBannerDataResult;
    '\n  *[_type == "footer" && _id == "footer"][0]{\n    _id,\n    subtitle,\n    backgroundImage {\n      \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n    },\n    columns[]{\n      _key,\n      title,\n      links[]{\n        _key,\n        name,\n        "openInNewTab": url.openInNewTab,\n        "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n      }\n    }\n  }\n': QueryFooterDataResult;
    '\n  *[_type == "navbar" && _id == "navbar"][0]{\n    _id,\n    columns[]{\n      _key,\n      _type == "navbarColumn" => {\n        "type": "column",\n        title,\n        links[]{\n          _key,\n          name,\n          icon,\n          description,\n          "openInNewTab": url.openInNewTab,\n          "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    )\n        }\n      },\n      _type == "navbarLink" => {\n        "type": "link",\n        name,\n        description,\n        "openInNewTab": url.openInNewTab,\n        "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    )\n      },\n      _type == "collectionGroup" => {\n        "type": "collectionGroup",\n        title,\n        "collectionLinks": collectionLinks[]->{\n          _id,\n          "slug": store.slug.current,\n          store{\n            title,\n            imageUrl\n          }\n        },\n        "collectionProducts": collectionProducts->{\n          _id,\n          "slug": store.slug.current,\n          store{\n            title\n          }\n        }\n      }\n    },\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n  }\n': QueryNavbarDataResult;
    '{\n  "page": *[_type == "page" && defined(slug.current)]{\n    "path": slug.current,\n    "lastModified": _updatedAt\n  },\n  "blog": *[_type == "blog" && defined(slug.current)]{\n    "path": slug.current,\n    "lastModified": _updatedAt\n  }\n}': QuerySitemapDataResult;
    '\n  *[_type == "settings"][0]{\n    _id,\n    _type,\n    siteTitle,\n    logo {\n      \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n    },\n    siteDescription,\n    socialLinks{\n      linkedin,\n      facebook,\n      twitter,\n      instagram,\n      youtube\n    }\n  }\n': QueryGlobalSeoSettingsResult;
    '\n  *[_type == "settings"][0]{\n    _id,\n    _type,\n    siteTitle,\n    siteDescription,\n    "logo": logo.asset->url + "?w=80&h=40&dpr=3&fit=max",\n    "socialLinks": socialLinks,\n    "contactEmail": contactEmail,\n  }\n': QuerySettingsDataResult;
    '\n  *[_type == "redirect" && status == "active" && defined(source.current) && defined(destination.current)]{\n    "source":source.current,\n    "destination":destination.current,\n    "permanent" : permanent == "true"\n  }\n': QueryRedirectsResult;
    '\n  *[_type == "redirect" && status == "active" && source.current == $source && defined(destination.current)][0]{\n    "source":source.current,\n    "destination":destination.current,\n    "permanent" : permanent == "true"\n  }\n': QueryRedirectBySourceResult;
    '\n  *[_type == "collectionsIndex"][0]{\n    ...,\n    _id,\n    _type,\n    title,\n    subtitle,\n    heroTitle,\n    heroImage {\n      \n  "id": asset._ref,\n  "preview": asset->metadata.lqip,\n  "alt": coalesce(\n    alt,\n    asset->altText,\n    caption,\n    asset->originalFilename,\n    "untitled"\n  ),\n  hotspot {\n    x,\n    y\n  },\n  crop {\n    bottom,\n    left,\n    right,\n    top\n  }\n\n    },\n    \n  buttons[]{\n    text,\n    variant,\n    _key,\n    _type,\n    "openInNewTab": url.openInNewTab,\n    "href": select(\n      url.type == "internal" => coalesce(\n        url.internal->slug.current,\n        "/collections/" + url.internal->store.slug.current\n      ),\n      url.type == "external" => url.external,\n      url.type == "email" => "mailto:" + url.email,\n      url.type == "product" => "/products/" + url.product->store.slug.current,\n      url.href\n    ),\n  }\n,\n    "slug": slug.current\n  }\n': QueryCollectionsIndexPageDataResult;
    '\n  *[_type == "bigcommerceCategory" && defined(store.slug.current) && store.isDeleted != true && store.isVisible == true]{\n    _id,\n    _createdAt,\n    "title": store.title,\n    "slug": store.slug.current,\n    "imageUrl": store.imageUrl,\n    "description": store.descriptionHtml\n  }\n': QueryAllCollectionsResult;
  }
}
